[high|build|build-infra] build_mmd_character_importer_exe.ps1:370 Default onefile build embeds 398 MB Blender zip, re-extracted to %TEMP% on every app launch
    DESC: The default build mode is --onefile (line 370) and Add-DataIfExists "blender-4.5.10-windows-x64.zip" "." (line 209) embeds the 398,911,842-byte Blender zip into the exe whenever it exists at repo root (it does on the dev machine — both committed-era .spec files show it in datas). PyInstaller onefile extracts ALL bundled data to a fresh %TEMP%\_MEIxxxx directory on every launch and deletes it on exit, so each app start unpacks ~400+ MB (zip + tool
    FIX: In onefile mode, skip bundling the Blender zip (rely on the documented auto-download / managed portable Blender under %LOCALAPPDATA%), or make -OneDir the default when the zip is present. Alternatively add a -BundleBlender opt-in switch instead of silently bundling based on file presence.

[high|bug|collision] blender_sort_collision.py:3338 Uncaught script exceptions exit Blender with code 0, so failures can be consumed as success (stale outputs)
    DESC: main() signals failure by raising (e.g. RuntimeError at lines 3224 and 3285 when collision validation fails, or any bpy/JSON error mid-run). Blender is launched by mmd_character_importer_core.py without --python-exit-code (verified: no match in tools/), and in --background mode an uncaught exception in a --python script prints a traceback but exits with code 0. run_process_streamed (core line 1274) only raises on nonzero exit, and the core wrappe
    FIX: In the __main__ block, wrap main() in try/except and call sys.exit(1) (SystemExit does propagate to Blender's process exit code) on any exception after printing the error; additionally have the core pass --python-exit-code 1 when launching Blender and delete/rename stale output JSONs before each run.

[high|perf|collision] blender_sort_collision.py:365 Model previews of up to 500k triangles serialized as indent=2 JSON with per-triangle duplicated strings
    DESC: write_json (line 363-365) always uses indent=2, which puts every float of every nested list on its own line. collect_model_preview (lines 796-869) samples up to MAX_PREVIEW_TRIANGLES = 500,000 triangles (line 30) and stores per-triangle dicts containing 9 coordinate floats, 6 UV floats, plus repeated long strings: material_uid, object_name, bodygroup, and the full texture_path (line 852) duplicated on every triangle. A typical 100-200k triangle M
    FIX: Serialize the preview payloads with separators=(",", ":") (no indent), drop texture_path/bodygroup/object_name from per-triangle records (they are derivable from material_uid via the materials table), and lower the preview triangle cap (e.g. 50-100k) or use flat coordinate arrays instead of per-triangle dicts.

[high|perf|core] mmd_character_importer_core.py:916 Plugin fingerprints based on absolute path break setup caching in onefile builds
    DESC: hash_file_metadata (lines 916-922) hashes str(path.resolve()) + size + mtime, and setup_state_is_current rejects the saved state when plugin_fingerprints() differs (line 1051). The shipped packaging is PyInstaller onefile (GmodSimpleMMDCharacterImporter.spec has EXE-only with no COLLECT; build_mmd_character_importer_exe.ps1 defaults to --onefile at line 370), where plugins_software/*.zip is extracted to a random per-run %TEMP%\_MEIxxxxxx dir, so 
    FIX: Fingerprint by file name + size only, or by content hash (file_sha1) computed once and cached; never include the absolute path or extraction mtime of bundled resources in the cache key.

[high|bug|flexes-import] blender_sort_flexes.py:984 Unhandled script exceptions exit Blender with code 0; stale outputs from a prior run are then treated as success
    DESC: All three scripts end with `raise SystemExit(main())` and never catch exceptions, and the core launches Blender without `--python-exit-code` (mmd_character_importer_core.py:3483-3502 and all other call sites). When main() raises (e.g. apply_plan's plan-validation RuntimeError at blender_sort_flexes.py:685, enable_cats failure in the other two scripts), background Blender prints the traceback and exits 0. The core only detects failure by checking 
    FIX: In each script wrap main() in try/except, print the traceback, and call sys.exit(1) (Blender propagates sys.exit codes from --python scripts), or add `--python-exit-code 1` to every Blender command in the core. Additionally, delete/overwrite the expected output files at the start of apply/analyze so stale artifacts can never satisfy the core's exis

[high|bug|gui-window-B] mmd_character_importer_gui.py:8283 _load_settings re-entrantly triggers save_settings, wiping saved settings on every launch
    DESC: All tab builders connect change signals to save_settings() BEFORE _load_settings() runs (__init__ builds tabs at lines 2983-2999, then calls _load_settings at 3004). Inside _load_settings, `self.main_gmod_row.set_value(main_gmod)` (line 8283) fires PathRow.changed (PathRow.edit.textChanged -> changed, line 612) which is connected to `lambda _value: self.save_settings()` (line 4762). Likewise `self.source_row.set_value(source)` (line 8301) fires o
    FIX: Add a `self._loading_settings = True/False` guard around the body of _load_settings and make save_settings() return immediately when it is set (mirroring the existing _loading_main_source_from_settings pattern, line 8274-8278, but applied globally). Alternatively use QSignalBlocker on each widget while restoring values.

[high|perf|gui-window-B] mmd_character_importer_gui.py:8936 analyze_main_pmx does full PMX parse, whole-file SHA-1, and second full parse for preview on the GUI thread
    DESC: analyze_main_pmx (8936-8979) runs synchronously on the Qt main thread: core.analyze_pmx (core lines 2037-2230) iterates EVERY vertex in pure Python computing bounds (core 2080-2085), every bone, every morph, and scans the source dir for textures; core.build_workspace (8952) computes file_sha1 of the entire PMX (core 2240, 905-913, chunked read of the whole file); load_main_preview (8999-9018) then re-parses the full PMX a second time to build the
    FIX: Move analyze_pmx + build_workspace + preview model loading into a QThread worker (the codebase already has ~35 such workers) and show an 'Analyzing...' state in main_stats_label; debounce combo/source changes so rapid switching does not queue redundant analyses. Cache the SHA-1 per (path, size, mtime) to avoid rehashing the same PMX on every select

[high|perf|gui-window-C] mmd_character_importer_gui.py:10365 Source-folder text change triggers recursive disk scan + full PMX parse + SHA1 + preview rebuild on the GUI thread
    DESC: PathRow.changed is wired to QLineEdit.textChanged (line 612) and source_row.changed connects to on_source_changed (line 5040). on_source_changed (10353) calls populate_pmx_files (10365), which runs core.find_pmx_files() — folder.rglob('*.pmx') with a stat() per file (core 4793-4796) — then analyze_current_pmx(silent=True) (10413-10444), which parses the whole PMX (core.analyze_pmx) AND computes a full SHA1 of the .pmx via core.build_workspace/fil
    FIX: Debounce the changed signal (e.g. 400 ms QTimer) or connect to editingFinished for typed input, and move find_pmx_files/analyze_pmx/build_workspace into a QThread worker like every other pipeline step. At minimum guard against scanning drive roots and show a busy cursor.

[high|perf|preview] model_preview.py:270 Full PMX parse runs on the GUI thread with per-field Python reads, freezing the UI for seconds
    DESC: load_static_preview_model parses every vertex in a pure-Python loop (lines 270-274), and each vertex costs ~10-16 method calls (handle.read + struct.unpack per scalar via core.PmxReader). Vertex counts up to core.MAX_SUPPORTED_PMX_VERTEX_COUNT = 300,000 are allowed, and the index array (line 277) is built with another per-element Python loop that can run millions of iterations. The GUI calls StaticModelPreviewWidget.load_model() directly on the m
    FIX: Move load_static_preview_model into a QThread worker (the GUI already has the pattern) and show a busy/progress state. Independently, vectorize the hot loops: vertex indices are fixed-size, so line 277 can be replaced with np.frombuffer(reader.read_exact(index_count * vertex_index_size), dtype=...) for an enormous speedup; reading the whole file in

[high|bug|proportion-release] blender_export_proportion_trick.py:1044 Script failures exit Blender with code 0; stale outputs make failed re-runs look successful
    DESC: The __main__ block (lines 1044-1046) only raises SystemExit(0) on success; any exception from run() (missing pelvis bone line 258, export failures line 549, verification failures lines 630-660) propagates as an uncaught Python exception. Blender invoked with --python but WITHOUT --python-exit-code (confirmed in mmd_character_importer_core.py run_proportion_export lines 3902-3925, and no occurrence of python-exit-code anywhere in tools/) prints th
    FIX: In the __main__ block, wrap main() in try/except, print the traceback, and call sys.exit(1) (SystemExit from a --python script does set Blender's process exit code). Additionally, delete stale report_json/files_json (and optionally pre_blend/processed_blend) at the start of run() so the core's existence checks cannot be satisfied by a previous run.

[high|bug|qc-compile] sort_qc_compile.py:199 Broken empty-path guard can lead to shutil.rmtree of the process CWD
    DESC: ensure_external_safe_qc_source() does `qc_dir = Path(str(plan.get("qc_dir") or ""))` ... `if not source_dir or not path_is_ascii(source_dir)`. `Path("")` constructs WindowsPath('.') which is ALWAYS truthy (verified: bool(Path(''))==True, str=='.', exists()==True), so `not source_dir` never fires and path_is_ascii('.') is True. A plan with a missing/empty "source_dir" therefore is NOT redirected to the safe staging dir. prepare_qc_source() (lines 
    FIX: Check the raw string instead of the Path: `raw = str(plan.get("source_dir") or "").strip(); if not raw or not path_is_ascii(raw): ...`. Additionally, in prepare_qc_source() refuse to rmtree when the resolved source_dir equals Path.cwd(), is a filesystem root, or is not inside external_tool_root()/the workspace.

[high|perf|vrd-carms] blender_sort_vrd.py:1188 Preview JSON serializes up to 9 full copies of the mesh with indent=2; 8 evaluated mesh extractions per run
    DESC: Every run mode collects 4 evaluated full-mesh frame previews twice: once for driver_frame_previews (run_analyze line 1350, run_apply 1417, run_preview 1487) and again inside build_vrd_preview -> collect_vrd_frame_previews (line 1188), i.e. 8 full depsgraph-evaluated to_mesh() dumps of up to MAX_PREVIEW_TRIANGLES=500000 triangles each (collect_preview, lines 283-378). The resulting preview JSON contains the sampled mesh 9 times (model_preview + fr
    FIX: Write compact JSON (separators=(',', ':')) for preview payloads; store per-frame data as vertex positions only and share topology/uvs/material_uid/texture_path from the base preview (the topology is identical across frames since only the armature pose changes); collect each frame once and derive both driver and final overlays from the same pass or 

[medium|perf|bodygroups] blender_sort_bodygroups.py:2861 Apply mode re-runs the full analysis including model preview and sklearn clustering, all discarded
    DESC: apply_plan() (lines 2861-2868) calls analyze_scene(), which unconditionally builds the 3D preview via collect_bodygroup_preview(sources) (line 2313, up to 500k sampled triangles with per-polygon material/texture lookups) and re-runs detect_split_candidates/OPTICS clustering (lines 2317-2318). In apply mode only analysis['sources'] (for the uid->object map) and the scale/separation reports are used; the preview and freshly detected candidates are 
    FIX: Add flags to analyze_scene (e.g. include_preview=False, detect_splits=False) and pass them from apply_plan so apply mode only performs scaling, pruning, material separation, and source collection.

[medium|perf|bodygroups] blender_sort_bodygroups.py:1222 Per-polygon material node-tree walks: material_texture_path/material_has_face_scale_hint evaluated for every polygon
    DESC: In collect_bodygroup_preview (lines 1221-1224), material_for_polygon, material_texture_path (which iterates the whole shader node tree), and material_color are computed for EVERY polygon, before the stride check at line 1230, so even skipped triangles pay the node-tree walk. Likewise current_face_top_z (lines 355-372) calls material_has_face_scale_hint(mat) per polygon, which calls material_texture_path again (lines 344-352); it runs twice per sc
    FIX: Precompute texture path / face-hint / color per material slot index once per object (dict keyed by poly.material_index) and look them up in the polygon loops; only do per-triangle work after the stride check.

[medium|perf|bodygroups] blender_sort_bodygroups.py:618 Shapekey delta scans use per-vertex Python Vector math and run 4+ times per mesh
    DESC: shapekey_max_delta (lines 618-631) loops over every vertex in Python creating Vector subtractions. It is invoked by fallback_prune_shapekeys for every key (lines 634-658), and prune_shapekeys runs both pre- and post-material-separation (analyze_scene lines 2307-2309), so unused keys (delta 0 everywhere, no early break) scan the full mesh each time. Then collect_sources calls shape_key_names(obj) (line 1103) and facial_shapekey_names(obj) (line 11
    FIX: Use key.data.foreach_get / relative.data.foreach_get into numpy arrays for delta computation, cache the per-key max-delta result on first computation, and have facial_shapekey_names reuse the shape_key_names result instead of recomputing.

[medium|perf|bodygroups] blender_sort_bodygroups.py:1215 connected_mesh_components recomputed from scratch for every object in preview and again at apply time
    DESC: collect_sources already computes full connected components with vertex indices per object (component_summaries(obj, include_indices=True), line 1116: union-find over all vertices plus world-space transform of every vertex). collect_bodygroup_preview then recomputes connected_mesh_components(obj, include_vertices=True) for the same objects (line 1215) just to build component_by_vertex. At apply time, add_temp_component_vertex_group (line 2492) and
    FIX: Compute components once per object in analyze_scene and pass the component/vertex maps to both collect_sources and collect_bodygroup_preview; invert the tracking_vertex_groups loop to a single pass over vertices building counts per group index.

[medium|bug|bodygroups] blender_sort_bodygroups.py:2854 Failures never set a non-zero exit code, and plan-validation failure writes no report JSON
    DESC: apply_plan raises RuntimeError on plan validation failure (lines 2854-2856) BEFORE any report JSON is written, and main() (lines 3130-3192) has no try/except that records errors. The core launches Blender without --python-exit-code (verified: no occurrence in tools/), so an unhandled Python exception still exits Blender with code 0. mmd_character_importer_core.sort_bodygroups_blend then only fails via the generic 'Blender completed but did not wr
    FIX: Wrap main() in try/except: on failure write a minimal report JSON with validation:{ok:false, errors:[...]} to --report-json (or --analysis-json in analyze mode) and call sys.exit(1); additionally have the core pass --python-exit-code 1 when launching Blender.

[medium|bug|bodygroups] blender_sort_bodygroups.py:27 Shapekey prune threshold 0.1 is applied in post-scale units but reported as meters; subtle morphs silently deleted (including in validate-manual)
    DESC: UNUSED_SHAPEKEY_DELTA_EPSILON = 1e-1 (line 27) is used after the ~40.457x character scale (analyze_scene scales at line 2306 before pruning at lines 2307-2309), so the effective cutoff is ~2.5mm of real morph movement, yet the report labels it 'movement_threshold_meters' (lines 637, 657, 666), which is wrong by the scale factor. Any vertex morph whose maximum delta is under ~2.5mm (subtle eyelid/pupil/teeth tweaks common in PMX models) is treated
    FIX: Use a genuinely-noise-level epsilon (e.g. 1e-4 in scaled units) for 'unused' detection or scale the threshold by the applied scale factor; fix the misleading movement_threshold_meters label; in validate-manual mode report would-be-pruned keys as warnings instead of removing them.

[medium|perf|bodygroups] blender_sort_bodygroups.py:1198 Preview cap of 500,000 triangle dicts can produce a 100+ MB analysis JSON
    DESC: collect_bodygroup_preview defaults max_triangles=500000 (line 1198) and emits one dict per sampled triangle containing 9 rounded floats, 6 UV floats, object name, full absolute texture path string, color, and ids (lines 1245-1257). The absolute texture path (often a long Unicode path) is duplicated into every triangle of that material, so a model near the cap yields an analysis JSON in the 100-300 MB range that Blender must serialize (write_json,
    FIX: Lower the default cap (preview rendering does not need more than ~50k triangles), and normalize per-material data (texture_path, color, object_name) into a lookup table referenced by index from flat vertex/uv arrays instead of per-triangle dicts.

[medium|perf|bones-spine] blender_fix_spine_bones.py:937 weighted_bone_names() full-mesh scan executed once per mapping entry
    DESC: In apply_renames, `source_had_weights = {source: source in weighted_bone_names() for source in mapping}` re-invokes weighted_bone_names() for EVERY source in the mapping. Each call iterates every vertex of every mesh and every vertex-group entry (lines 123-133). With a typical spine plan of ~5-7 renames and a 200k+ vertex MMD model this performs 5-7 redundant full scans (plus the legitimate final scan at line 963), adding many seconds of pure-Pyt
    FIX: Hoist the scan: `weighted = weighted_bone_names(); source_had_weights = {source: source in weighted for source in mapping}`.

[medium|bug|bones-spine] blender_sort_bones.py:470 Early-stop guard is dead in round 1, so all accessory chains are decimated regardless of how far over the limit the model is
    DESC: In automatic_plan, the chain-decimation loop breaks when `len(existing) - len(round_ops) <= limit and operations`. `operations` is the cumulative list that is only extended AFTER a round completes (line 496), so during round 1 it is always empty and the guard never fires. A model that is only 1-2 bones over the 254 limit still gets every linear chain >=3 bones (hair/skirt/physics chains) halved in round 1, merging dozens of weighted bones and deg
    FIX: Make the guard `if len(existing) - len(round_ops) <= limit and (operations or round_ops): break` (or simply drop the `and operations` clause), and also check the projected count before scheduling each individual chain op rather than once per chain.

[medium|bug|bones-spine] blender_fix_spine_bones.py:697 Clavicle fallback matching has no left/right disambiguation; duplicate selection is then silently dropped by dict overwrite
    DESC: For attachment targets the fallback hints are `(target.rsplit("_", 1)[-1], target)`, i.e. for ValveBiped.Bip01_R_Clavicle just "Clavicle" plus the full name. exact_or_named_candidate (lines 280-293) scores any bone containing "clavicle" identically for both the R_ and L_ targets and breaks ties alphabetically (line 292), so bones like "clavicleL"/"clavicleR" map the SAME source (alphabetically first) to both targets. proposal_for_armature only em
    FIX: Include side tokens in the hints (e.g. require "r"/"right"/"右" vs "l"/"left"/"左" or compare head_local.x sign for L/R targets), exclude already-used sources when selecting attachments, and make source_mapping_from_plan raise on duplicate source keys instead of overwriting.

[medium|perf|bones-spine] blender_sort_bones.py:1200 Apply mode performs ~8 redundant full per-vertex scans and embeds two full model previews in the report JSON
    DESC: Apply mode calls analyze_current_file twice (lines 1200 and 1217) purely to embed "before"/"after" blobs in the report. Each call runs vertex_group_weight_totals (full scan of every vertex/group, line 612), automatic_plan which scans again (line 444), and collect_model_preview which materializes ALL vertices and edges as Python lists before sampling up to 500,000 of each (lines 260-312). apply_plan adds two more scans (lines 1097, 1070). That is 
    FIX: Skip model_preview (and the redundant automatic_plan re-run) in the before/after blocks of the apply report, or reference the already-written analysis JSON instead of embedding it; reuse a single weight-totals scan across analyze/apply/validate, and use mesh foreach_get for preview extraction.

[medium|build|build-infra] .gitignore:21 /reference is gitignored while the build script hard-requires 10 reference/ paths
    DESC: Line 21 ignores the entire /reference tree, yet build_mmd_character_importer_exe.ps1 lines 213-222 call Add-RequiredData on reference\ref_motion, reference\proportion_trick_script-main_new\..., reference\li_zhiyan_npc\..., reference\!enhanced_animation_importer_arc\tools and reference\dynamic_model_importer (throwing if missing), and sync_github_upload.ps1 lines 137-151 require the same. The 47 currently tracked reference files survive only becau
    FIX: Replace the blanket /reference ignore with targeted ignores for the genuinely unwanted subfolders (e.g. /reference/!runtime_any_model_importer), or add negation patterns (!/reference/ref_motion/ etc.) for every path the build script requires; delete the redundant lines 19-20.

[medium|perf|build-infra] download_build_assets.ps1:50 398 MB download via Invoke-WebRequest without progress suppression or -UseBasicParsing is very slow / fragile under Windows PowerShell 5.1
    DESC: The repo README explicitly tells users to run this via 'powershell -ExecutionPolicy Bypass -File .\scripts\download_build_assets.ps1', i.e. Windows PowerShell 5.1. There, Invoke-WebRequest -OutFile (line 50) buffers the whole 398,911,842-byte body in memory and renders a progress bar per read, which is well known to slow large downloads by an order of magnitude (often 10x+); without -UseBasicParsing, 5.1 can also fail outright on machines where t
    FIX: Set $ProgressPreference='SilentlyContinue' before the download, add -UseBasicParsing for 5.1 compatibility, OR-in Tls13 to the protocol mask (or leave SystemDefault on PS7), and consider Start-BitsTransfer or HttpClient streaming with a simple retry for the 398 MB asset.

[medium|perf|collision] blender_sort_collision.py:2557 build_collision_parts always builds both filtered and unfiltered full source indexes up front
    DESC: Lines 2557-2558 call build_collision_source_index twice (filtered_sources=True and False). Each call does a complete pass over every vertex of every mesh, accumulating per-target weight lists ([0.0]*len(vertices) for each of up to 32 targets per object) and copying all coordinates into Vectors. The unfiltered index is only consulted as a sparse-bone fallback (lines 2598-2600, 2612-2615). When no explicit source-bodygroup selection is active (ACTI
    FIX: Build the unfiltered index lazily only when a bone's filtered cloud/region is actually sparse, and skip it entirely when the filtered object set equals the full object set (no selection active and no excluded-token matches).

[medium|perf|collision] blender_sort_collision.py:898 collect_collision_source_bodygroups does O(vertices x targets) weight lookups, rebuilding a set per call
    DESC: For every vertex of every object, lines 898-905 call vertex_group_weight_any once per target bone (~18-32 targets). vertex_group_weight_any (lines 1211-1217) rebuilds wanted = set(bone_names) and re-iterates vertex.groups on every call. For a 150k-vertex model this is ~3-5 million Python function calls plus the same number of set constructions, just to compute per-bodygroup weighted-vertex counts. This runs in scan-sources mode (line 931) every t
    FIX: Precompute group_name -> [target] mapping per object (as build_collision_source_index does) and accumulate target hits in a single pass over vertex.groups per vertex; also hoist the set(bone_names) construction out of vertex_group_weight_any by accepting a precomputed set.

[medium|perf|collision] blender_sort_collision.py:2760 Identical full model preview recomputed and embedded in all three scan/analyze outputs
    DESC: scan_source_bodygroups (line 929), scan_collision_bones (line 576) and analyze_scene (line 2760) each call collect_model_preview() and embed the full triangle preview plus materials list into their output JSON (lines 612-614, 945-947, 2806-2808). The core launches a separate Blender process for each mode (core lines 3560, 3610, 3674), so opening the Step-8 dialog and analyzing pays the full preview build (a complete pass over all polygons with wo
    FIX: Write the model preview once to a shared sidecar file keyed by the input blend's path+mtime and have scan-sources/scan-bones/analyze reuse it (or accept a --skip-preview flag the core sets when a fresh preview already exists).

[medium|bug|collision] blender_sort_collision.py:1103 Hardcoded 'physics' name-token exclusion silently overrides explicit user bodygroup selection
    DESC: is_collision_source_object (lines 1101-1107) checks EXCLUDED_SOURCE_OBJECT_TOKENS before the explicit selection: any object whose sanitized name contains 'physics' returns False even when the user explicitly enabled it in ACTIVE_SOURCE_BODYGROUPS. The selectable list shown to the user is built from mesh_objects() in collect_collision_source_bodygroups (line 888), which only excludes the object literally named 'Physics' — so a bodygroup like 'phys
    FIX: When ACTIVE_SOURCE_BODYGROUPS is not None, honor the explicit selection (skip the token check, or apply tokens only to compute the default selection); alternatively mark token-excluded objects as enabled=False with a warning in collect_collision_source_bodygroups so the GUI reflects reality.

[medium|bug|core] mmd_character_importer_core.py:1242 Cancel is only honored after the subprocess prints another line
    DESC: run_process_streamed checks cancel_check only at the top of the loop, then blocks in process.stdout.readline() (line 1242). During long silent stretches (CoACD collision analysis, Cycles icon render, studiomdl compile, mmd_tools import phases) no line arrives for minutes, so the GUI Cancel button (every worker passes cancel_check, e.g. gui lines 669-2483) appears dead: the Blender process keeps running until it next writes a line or exits. The ti
    FIX: Read stdout on a dedicated drain thread into a queue.Queue and have the main loop do queue.get(timeout=0.1) so cancel_check is evaluated every ~100 ms regardless of subprocess output; or use Popen with a non-blocking pipe reader.

[medium|bug|core] mmd_character_importer_core.py:1234 Cancelling kills only the direct child; studiomdl/VTFCmd grandchildren are orphaned
    DESC: On cancel, run_process_streamed calls process.terminate()/kill() (lines 1234-1238), which on Windows is TerminateProcess on the direct child only. Steps 12/13/14 launch python wrappers (sys.executable + sort_qc_compile.py / sort_param_textures.py / sort_icons_and_arts.py, lines 4255/4309/4489/4617) that themselves spawn studiomdl.exe (sort_qc_compile.py:1931, 2685), VTFCmd.exe (sort_qc_compile.py:2425) and gmad.exe. Cancelling these steps orphans
    FIX: Kill the whole tree on cancel: create the child with creationflags=CREATE_NEW_PROCESS_GROUP and run 'taskkill /PID <pid> /T /F', or assign the child to a Windows Job Object with JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE.

[medium|bug|core] mmd_character_importer_core.py:2086 PMX model height measured along Z (depth) instead of Y (up axis)
    DESC: analyze_pmx computes model_height = vertex_bounds_max[2] - vertex_bounds_min[2] (lines 2086-2090). PMX files are Y-up (Blender's Z-up conversion happens later in mmd_tools), so index 2 is the model's front-back depth, typically 5-10x smaller than its height. detect_parenting_warning_bones (lines 1906-1972) derives side_threshold = model_height * 0.15 and extreme_threshold = max(model_height * 0.9, p95 * 5.0) from this value, so the new bone-paren
    FIX: Use axis index 1 (Y) for the height extent in analyze_pmx, or compute the max extent across all three axes as a robust proxy.

[medium|perf|core] mmd_character_importer_core.py:1542 Recursive rglob over every Steam library to find blender.exe
    DESC: _system_blender_candidates appends every Steam library's steamapps/common to search_roots and runs root.rglob("blender.exe") over it (lines 1536-1543). That walks the user's entire installed game collection (easily 100k+ files across multiple drives, minutes on HDDs), then runs 'blender --version' (30s timeout each) for every hit. This runs inside warn_about_system_blender (line 1594) on the first ensure_portable_blender call of each session and 
    FIX: Probe only the known fixed location steamapps/common/Blender/blender.exe per library instead of rglob, and/or run the warning scan on a background thread that never blocks step execution.

[medium|bug|core] mmd_character_importer_core.py:1164 Interrupted Blender extraction is later reused as a valid install
    DESC: extract_blender extracts directly into the final software/blender/<version> directory and on the next call returns early if find_blender_exe_in_dir finds any blender.exe there (lines 1163-1167). If extraction is interrupted (app killed, crash, disk full mid-way), blender.exe typically already exists while DLLs/python libs are missing, so subsequent runs emit 'Using existing portable Blender' and launch a broken install - producing exactly the 0xC
    FIX: Extract into a temp sibling directory and os.replace it into place on success, or write a small extraction-complete marker file and treat installs without it as invalid in find_blender_exe_in_dir/extract_blender.

[medium|bug|flexes-import] blender_import_mmd_model.py:36 Import call_operator ignores CANCELLED, so a failed CATS import saves an empty blend and 'succeeds'
    DESC: Unlike blender_fix_mmd_model.py's call_operator (lines 60-70), which raises when the operator returns {'CANCELLED'}, the import script's call_operator (lines 36-41) returns the result unchecked. If bpy.ops.cats_importer.import_any_model returns {'CANCELLED'} (corrupt/unsupported PMX, internal mmd_tools error reported via self.report), import_with_cats (lines 128-131) proceeds, the script saves an empty scene and writes a report with object_count 
    FIX: Add the same CANCELLED check as the fix script's call_operator, and after import fail fast with a clear error if bpy.data.objects contains no ARMATURE or no MESH (or vertex_count == 0) before saving the blend.

[medium|perf|flexes-import] blender_sort_flexes.py:561 Preview generation does per-vertex matrix allocation in a triple-nested loop and emits a bloated indented JSON
    DESC: collect_flex_preview (lines 505-597) iterates triangles x flex shapekeys x 3 vertices in pure Python, and calls obj.matrix_world.to_3x3() inside the innermost vertex loop (line 561), allocating a fresh Matrix per vertex per key per triangle — for a 100k-triangle face mesh with 30 flexes that is ~9M Matrix allocations plus Vector math, taking minutes of analyze time. Each emitted triangle dict also duplicates per-material data (texture_path which 
    FIX: Hoist matrix_world and its 3x3 outside the loops; pull coordinates and shapekey data in bulk via foreach_get into flat arrays; reference material metadata by uid only (texture path/color already live in the materials table); and write the preview without indentation (or as a separate compact file) to cut size several-fold.

[medium|perf|flexes-import] blender_sort_flexes.py:438 Analyze pass re-scans every vertex multiple times: max-delta computed twice per shapekey, average Z recomputed per sort
    DESC: shape_key_max_delta (lines 278-289) walks all vertices in Python; collect_flexes calls it twice for every shapekey — once as the skip filter (line 415) and again for the report field (line 438) — doubling an already O(keys x verts) cost. object_average_world_z (lines 223-230) also walks all vertices doing matrix_world @ co per vertex, and is recomputed for each object in three separate sorted(..., key=bodygroup_processing_key) calls (lines 409, 5
    FIX: Compute shape_key_max_delta once per key and reuse the value (the line-415 filter and line-438 field can share it); cache object_average_world_z per object (compute once into a dict and key the sorts off it); and use foreach_get into flat float arrays / numpy for the delta and average-height math.

[medium|perf|flexes-import] blender_sort_flexes.py:707 apply_plan materializes per-vertex Vector delta lists for every shapekey of every mesh (GB-scale memory)
    DESC: Lines 698-708 build raw_by_object_name: for every mesh and every shapekey, a Python list of len(vertices) mathutils.Vector objects (key.data[i].co - basis[i]). A mathutils Vector plus list slot costs roughly 100+ bytes, so a CATS-joined body mesh of 200k vertices with 60 morphs allocates ~12M Vectors (>1 GB) before any work happens, in the same Blender process that already holds the mesh data. Deltas are captured for all keys even though only key
    FIX: Capture deltas lazily only for shapekeys actually referenced by plan entries/merge sources, and store them as flat float arrays via key.data.foreach_get / numpy so the accumulation, rest-offset, and rewrite loops (lines 832-833, 874-875) become vectorized bulk operations with foreach_set.

[medium|bug|gui-helpers-workers] mmd_character_importer_gui.py:2283 FullImportWorker._optional swallows cancellation: cancel during steps 10-13 logs warnings and continues to later steps
    DESC: core.run_process_streamed raises RuntimeError('operation was cancelled') when cancel_check fires (verified in mmd_character_importer_core.py lines 1233-1273). FullImportWorker._optional (lines 2283-2290) catches ALL exceptions from optional steps 10 (c_arms), 11 (VRD), 12 (textures), 13 (icons) and records them as warnings, then continues. If the user clicks Cancel while one of those steps runs, the cancellation is recorded as e.g. 'Optional Step
    FIX: In _optional, check self.cancel_requested after catching the exception (or detect the 'operation was cancelled' RuntimeError) and re-raise instead of recording a warning; also special-case cancellation in the outer except of run() to emit a clean 'Import cancelled' message instead of 'Main import failed during Step N'.

[medium|bug|gui-window-A] mmd_character_importer_gui.py:3747 step_to_tab_index falls back to step number as tab index and depends on translated tab-text prefix; breaks navigation and corrupts tab labels in French
    DESC: step_to_tab_index (lines 3742-3747) finds a step's tab by checking tabText(index).startswith(f"{step} ") and on failure returns int(step) as the tab INDEX. That fallback is always wrong: tab index = step + 1 because 'Main Import to GMod' and 'Model Manager' occupy indices 0-1. The prefix match runs against TRANSLATED tab text (apply_workflow_i18n line 3473-3474 and apply_global_i18n rewrite tab titles), so any translation that does not start with
    FIX: Store a step->tab-widget (or step->index) mapping when the tabs are created (e.g. tabBar().setTabData(index, step) or a dict keyed by the content widget like tab_index_for_content_widget already does) instead of parsing display text. At minimum change the fallback to return -1 and guard callers, and fix fr_fr.json step_02 to "2 Modèle correctif".

[medium|perf|gui-window-A] mmd_character_importer_gui.py:4192 refresh_workflow_statuses does hundreds of synchronous filesystem operations on the GUI thread and is retriggered on every keystroke in 14 path fields
    DESC: For each of 15 steps, refresh_workflow_statuses calls workflow_state_for_step -> step_is_complete (step_output_dir + core.is_step_complete marker JSON read + try_backfill_step_completion, which runs step_expected_outputs with multiple step_dir.glob() scans at 4116-4118 and parses report JSON at 4121-4127), then calls first_missing_dependency AGAIN at line 4203 (already computed inside workflow_state_for_step at 4188), then step_is_complete for ea
    FIX: Coalesce refreshes with a single-shot debounced QTimer (e.g. 250 ms, restarted on each request) and a 'pending' flag; cache step_is_complete/step_output_dir results per refresh pass (compute once per step into a dict) instead of recomputing 3-4 times; consider moving marker/glob probing into a worker thread or only probing the currently visible ste

[medium|bug|gui-window-A] mmd_character_importer_gui.py:3402 Language switch reverts dynamic label text to its startup snapshot, wiping preflight stats, summaries, and progress text
    DESC: _widget_source_text (3402-3409) permanently captures a widget's CURRENT text as its '_mci_i18n_text' source property the first time apply_global_i18n runs (at startup, line 3005). For labels whose text changes at runtime (main_stats_label 'Select a PMX and run preflight.', stats_label, bodygroup/flex/collision summary labels, progress labels 'Idle', preview status labels), the captured startup string is re-applied by apply_global_i18n (3420-3439)
    FIX: Mark dynamic labels explicitly (e.g. setProperty('_mci_i18n_skip', True) at creation, checked in apply_global_i18n), or only capture sources during the first apply_i18n pass at startup and never re-apply to widgets whose current text no longer equals the last text the i18n system itself set.

[medium|bug|gui-window-A] mmd_character_importer_gui.py:2862 No closeEvent: closing the window mid-run leaves QThread workers and Blender subprocesses unmanaged
    DESC: ImporterWindow holds ~35 QThread worker references (self.worker, update_check_worker, workspace_size_worker, model_manager_scan_worker, etc., lines 2871-2917) but the class defines no closeEvent anywhere in the 18k-line file (verified by grep). If the user closes the main window while a long step (Blender import, CoACD, studiomdl compile) is running, the QThread objects are destroyed while still running — Qt emits 'QThread: Destroyed while thread
    FIX: Implement closeEvent: if any worker isRunning(), show a confirm dialog; on accept, call each worker's cancel() (which should terminate child processes), wait() with a timeout, then save_settings() and accept the event.

[medium|perf|gui-window-B] mmd_character_importer_gui.py:8871 populate_main_pmx_files performs unbounded recursive disk scan on the GUI thread
    DESC: populate_main_pmx_files (8864-8894) calls core.find_pmx_files(source) synchronously; that function does `folder.rglob('*.pmx')` over the entire selected tree plus a `stat()` per match for size sorting (core 4793-4796). It runs on every change of the model-folder PathRow (via on_main_source_changed, 8847-8862) and at startup. If the user picks a broad folder (Downloads, a drive root), the UI hard-freezes for the duration of the walk with no feedba
    FIX: Run find_pmx_files in a worker thread with a 'Scanning...' label, or at minimum bound the recursion depth / file count and debounce the changed signal (the scan currently fires on every keystroke typed into the folder edit because PathRow emits changed on textChanged, line 612).

[medium|perf|gui-window-B] mmd_character_importer_gui.py:8602 save_settings writes ~70 keys and flushes QSettings to the registry on every keystroke
    DESC: save_settings (8480-8602) unconditionally setValue()s roughly 70 keys and ends with settings_store.sync() (8602), which forces a flush to the Windows registry. It is connected to textChanged of many line edits — qc fields (7932-7938), icon_basename (7563), release metadata edits (8193-8204, which additionally run commit_release_plan_from_ui per keystroke), VMD/image PathRows (7560-7562) — and to spin/checkbox changes (6770, 6930). qc_category_edi
    FIX: Drop the explicit sync() (QSettings flushes automatically on a timer and at destruction), or debounce saves with a single-shot QTimer (e.g., 500 ms after the last change), or connect editingFinished instead of textChanged for line edits.

[medium|bug|gui-window-C] mmd_character_importer_gui.py:11640 detect_step4_output can select the unsorted manual-merge .blend instead of the bone-sorted output
    DESC: detect_step4_output globs '*.blend' in the step-4 dir (lines 11640 and 11643: Path(sort_dir_text).glob('*.blend') and workspace.glob('4_sort_bones/*.blend')) and takes the first existing candidate in sorted() (lexical) order. prepare_sort_manual_merge_blend (11130-11146) always creates '<stem>_bones_manual_merge.blend' in 4_sort_bones during every Analyze (it is a verbatim copy of the step-3 input, see core.sort_bones_manual_merge_path_for_spine_
    FIX: Glob the specific output pattern '*_bones_sorted.blend' (matching the completion pattern at line 4103) instead of '*.blend', or explicitly exclude '*_bones_manual_merge.blend' candidates.

[medium|bug|gui-window-D] mmd_character_importer_gui.py:13955 Apply 'complete with warnings' path leaves Step 7/8 stuck in 'Working' state with no error details
    DESC: start_flex_apply calls set_step_working(7) (line 13911) which sets workflow_states[7]='working'. In flex_apply_done, when report validation is not ok (else branch, lines 13955-13957) the code only sets the progress label to 'Flexes Complete With Warnings' and returns: it never calls clear_step_state, set_step_failed, or complete_step_from_result, so the workflow panel shows the step as amber 'Working' (refresh_workflow_statuses, line 4196) indefi
    FIX: In the warnings branch, call set_step_failed(7/8, ...) (or clear_step_state) and show the validation issues extracted from report['validation'] via show_error or a warning dialog, matching how validate_flex_plan errors are presented before apply.

[medium|perf|gui-window-D] mmd_character_importer_gui.py:15932 Huge vrd_preview.json parsed synchronously on the GUI thread whenever the Step 11 input changes
    DESC: update_vrd_output_preview (called from on_vrd_input_changed, i.e. on every path edit, browse, detect, or settings restore) does `json.loads(preview_json.read_text(...))` on the main thread (line 15932), and vrd_apply_done has the same fallback at lines 16052-16057. blender_sort_vrd.py writes vrd_preview.json containing the base model_preview sampled up to MAX_PREVIEW_TRIANGLES = 500,000 triangles (blender_sort_vrd.py lines 20, 370-373) plus full 
    FIX: Move these JSON loads into a small QThread worker (the codebase already has 35 of them) or defer/lazy-load the preview payload; at minimum show a busy cursor and load the preview JSON only when the VRD preview widget is actually visible.

[medium|perf|gui-window-D] mmd_character_importer_gui.py:16128 VRD intensity sliders re-blend the entire triangle preview on every drag tick
    DESC: Each intensity slider is connected via valueChanged (line 7008), which fires continuously while dragging. on_vrd_intensity_changed (lines 16128-16135) calls refresh_vrd_preview, which calls blended_vrd_model_preview (lines 16485-16519): a pure-Python loop over every preview triangle (up to 500,000 per the MAX_PREVIEW_TRIANGLES cap in blender_sort_vrd.py) creating a new dict and lerping 3 points per triangle, then re-uploads the whole model to the
    FIX: Debounce the recompute (QTimer.singleShot ~100ms after the last tick, or connect the full rebuild to sliderReleased and only update the multiplier label during drag), and/or vectorize the point blending with numpy arrays kept resident instead of per-triangle dicts.

[medium|bug|gui-window-D] mmd_character_importer_gui.py:15451 Workspace-cache cleanup and Blender-cache deletion can run concurrently with workflow steps
    DESC: clear_workspace_cache (line 15451) and delete_managed_blender_cache (line 15531) refuse to start while self.worker.isRunning(), but the guard is one-directional: every step starter (e.g. start_flex_apply line 13891, start_collision_apply line 14982, start_proportion_run line 15204) only checks self.worker, not workspace_cleanup_worker or managed_blender_delete_worker. While WorkspaceCleanupWorker is shutil.rmtree-ing the entire workspaces root (l
    FIX: Add the cleanup/delete workers to the busy check used by ensure_step_can_run / the start_* methods (e.g. a single is_busy() helper that checks self.worker, workspace_cleanup_worker, and managed_blender_delete_worker), and disable the step Run buttons while cleanup runs.

[medium|perf|gui-window-E] mmd_character_importer_gui.py:17095 populate_texture_table blocks the GUI thread with per-row recursive disk scans and full-size image decodes
    DESC: populate_texture_table runs on the main thread and, for every row, calls resolve_texture_base_path_for_display (17046-17086) which — whenever the recorded base path is missing (typical after a workspace was moved/renamed, i.e. when ALL rows are missing) — does folder.rglob('*') over the entire 0_source_mmd_assets tree per row (line 17069), with no shared file-list cache: O(rows x files) full-disk rescans. It then calls set_texture_item_icon (1728
    FIX: Build the candidate file index once (single rglob, dict keyed by casefolded stem) and reuse it for all rows; use QImageReader with setScaledSize for thumbnails, or generate icons lazily/in a thread.

[medium|perf|gui-window-E] mmd_character_importer_gui.py:17425 save_settings (~50 QSettings writes + sync) called on every icon preview frame change
    DESC: on_icon_frame_spin_changed (17393), on_icon_preview_slider_changed (17412) and on_icon_animation_preview_frame_changed (17425, connected to preview.frameChanged at 17474) each call self.save_settings(). save_settings (8480-8602) writes ~50 values to QSettings (Windows registry) and ends with settings_store.sync(). Dragging the preview frame slider or playing the VMD animation preview fires these handlers continuously, producing dozens of full reg
    FIX: Debounce: persist the frame value via a single-shot QTimer (e.g. 500 ms after the last change), or only save on editingFinished / sliderReleased.

[medium|bug|gui-window-E] mmd_character_importer_gui.py:16614 add_vrd_row can generate duplicate uids after rows are removed
    DESC: add_vrd_row builds the uid as f"manual_vrd_{len(rows) + 1:03d}" (line 16614). Because it is based on the current row count, the sequence add → remove a different row (remove_selected_vrd_rows, 16635) → add produces a uid that collides with an existing manual row (e.g. two rows named manual_vrd_011). populate_vrd_table keys _vrd_entries_by_uid on uid (16191-16197), so with duplicates both table rows map to one plan entry: edits land on the wrong r
    FIX: Generate a uid that is unique against the existing rows, e.g. increment a counter until f"manual_vrd_{n:03d}" is not in {row['uid']}, or use uuid4().hex[:8].

[medium|bug|icons-addons] blender_render_icon.py:493 Texture alpha handling is dead code: Material Output node has no 'Alpha' input, so transparent MMD textures render opaque in icons
    DESC: make_unshaded_materials() guards the alpha hookup with `if "Alpha" in tex.outputs and "Alpha" in output.inputs:` (line 493). ShaderNodeOutputMaterial has never had an 'Alpha' input in any Blender 2.8x-4.x version (inputs are Surface/Volume/Displacement/Thickness), so this branch never executes: the image alpha is never connected to anything and `material.blend_method = "BLEND"` (line 496) is never set. Every material is reduced to an opaque Emiss
    FIX: Route transparency through the shader instead of the output node: link tex Alpha -> Mix Shader factor between a Transparent BSDF and the Emission shader (or use a Principled BSDF with Emission color + Alpha input), then set material.blend_method = 'HASHED'/'CLIP' for Blender <=4.1 and material.surface_render_method = 'BLENDED' (or 'DITHERED') for 4

[medium|bug|icons-addons] blender_render_icon.py:713 render_icon() can report false success from a stale output file of a previous run
    DESC: render_icon() never deletes a pre-existing output PNG and never checks the bpy.ops result set; success is detected solely via `output_path.exists()` (lines 713, 722, 732). The output paths (13_sort_icons_and_arts/release_icon.png and spawn_source.png) persist across runs, and re-running Step 13 with a different frame/VMD is a supported workflow (run_icons in mmd_character_importer_core.py re-invokes the script into the same icon_dir). If a re-run
    FIX: At the top of render_icon(), delete output_path if it exists (output_path.unlink(missing_ok=True)). Additionally capture the operator result (result = bpy.ops.render.render(write_still=True)) and treat anything other than {'FINISHED'} as failure, and/or compare the file mtime against a timestamp taken before rendering.

[medium|bug|icons-addons] sort_icons_and_arts.py:88 ASCII staging root for VTFCmd lives under %LOCALAPPDATA%, which is itself non-ASCII for non-ASCII Windows usernames
    DESC: convert_to_vtf() stages images to an ASCII-safe scratch dir when the workspace path is non-ASCII (lines 343-356), because VTFCmd (VTFLib, ANSI file APIs) fails on paths outside the active codepage. But external_tool_root() (lines 87-89) builds that staging root from LOCALAPPDATA/TEMP/home -- all of which sit under C:\Users\<username>\... For a user whose Windows username is Japanese/Chinese/Cyrillic (a core audience for an MMD importer), the 'ASC
    FIX: Validate path_is_ascii(external_tool_root()) and fall back to a guaranteed-ASCII location such as C:\Windows\Temp\MMDCharacterImporter or the drive root, or convert the staging path with the Win32 GetShortPathNameW (8.3) form before handing it to VTFCmd.

[medium|perf|materials-textures] blender_sort_materials.py:271 Per-pixel pure-Python alpha counting over full-resolution image buffers
    DESC: image_alpha_stats() (lines 269-271) does pixels = array('f',[0.0])*(total*4); image.pixels.foreach_get(pixels); then sum(1 for offset in range(3, len(pixels), 4) ...). Accessing image.pixels forces Blender to materialize the full float buffer (a 4096x4096 texture is 268 MB), and the zero-alpha count then iterates 16.7M times in pure Python (seconds per image). This runs for every material's base texture in scan mode AND again in the after-analysi
    FIX: Use numpy (bundled with Blender): buf = np.empty(total*4, dtype=np.float32); image.pixels.foreach_get(buf); zero = int((buf[3::4] <= TEXTURE_ALPHA_ZERO_THRESHOLD).sum()). Optionally call image.buffers_free() (or image.gl_free()) after to release the float buffer, and skip recomputation in the after-analysis passes where the stats are not used for k

[medium|perf|materials-textures] blender_sort_materials.py:1766 consolidate_merge_groups scans all entries for every polygon (O(polys x materials))
    DESC: In consolidate_merge_groups, the per-polygon loop (lines 1761-1770) resolves a material uid by iterating the entries list and rebuilding 'current = str(entry.get("current_name") or ...)' for every entry, for every polygon. A 200k-polygon model with 60 merge entries performs ~12M Python iterations with string construction/comparison each. Ironically, uid_by_slot (built at lines 1724-1737, keyed by (object, slot_index)) already provides this mappin
    FIX: Before the polygon loop, build name_to_uid = {current_name: uid} once (or reuse uid_by_slot.get((obj.name, poly.material_index))), then resolve each polygon's uid with a dict lookup instead of scanning entries per polygon.

[medium|perf|materials-textures] blender_sort_materials.py:1657 apply/merge reports embed the full 500k-triangle model preview in indent=2 JSON
    DESC: apply_initial (line 1623) and apply_merge (line 1941) call analyze_current_file(), whose result includes model_preview from collect_material_preview() (lines 596-657): up to 500,000 triangle dicts, each carrying 9 rounded coords, 6 UVs, a color, the material uid, AND the absolute texture_path string repeated per triangle. This entire structure is embedded under report['after'] (lines 1657 and 1963) and written with json.dumps(indent=2) (write_jso
    FIX: Strip model_preview from the 'after' analysis embedded in reports (write it to a separate preview file only if the GUI needs it), reference texture paths once per material uid instead of per triangle, and write large payloads without indent.

[medium|bug|materials-textures] blender_sort_materials.py:1483 Stacked-face offset moves both coincident layers identically, leaving same-orientation duplicates still z-fighting
    DESC: detect_and_offset_stacked_material_groups adds the face indices of BOTH matched groups to faces_to_move (lines 1483-1484), and offset_faces_for_object then displaces every affected vertex along its accumulated face normal by the same +0.0001 (lines 1387-1391). signatures_match accepts both same-direction and flipped normals (abs(dot) >= 0.999, line 1299). For inside/outside clothing layers (opposite normals) the two layers move apart, but for exa
    FIX: For each stacked pair, offset only one group (e.g. the second by uid order), or move the two groups in opposite directions (+offset / -offset) when their normals are parallel rather than anti-parallel.

[medium|bug|materials-textures] blender_sort_materials.py:1536 Merge grouping merges far below the material limit (33 materials with limit 32 -> 17 groups)
    DESC: build_merge_plan_from_analysis computes group_size = ceil(count / limit) and assigns groups sequentially (lines 1536-1539). The resulting final count is ceil(count / group_size), which can be drastically below the limit: count=33, limit=32 gives group_size=2 and only 17 final materials; count=40 gives 20. Every unnecessary merge atlases two unrelated textures together (quality loss, larger atlas pages) when the limit would have allowed keeping mo
    FIX: Distribute materials into exactly min(count, limit) groups with balanced sizes (e.g. group index = floor(i * limit / count)), so 33 materials with limit 32 yields 32 groups with a single merged pair.

[medium|bug|materials-textures] blender_sort_materials.py:1103 Material renames can silently collide with existing datablocks, producing '.001' names that break the merge stage
    DESC: rename_target_materials (line 1103) and consolidate_merge_groups (line 1797) assign mat.name = candidate while only deduplicating against names assigned within the same loop ('used' set). Unkept/merged-away materials are never deleted from bpy.data.materials, and other targets still hold their pre-rename names, so if the new name matches ANY existing datablock Blender silently renames to 'name.001'. Consequences: (a) in merge mode validate_after_
    FIX: Do a two-phase rename (first rename all targets to unique temporary names, then to final names), or check bpy.data.materials for the candidate name and free/rename the conflicting datablock first; also assert mat.name == candidate after assignment and fail loudly on mismatch.

[medium|perf|materials-textures] sort_param_textures.py:377 Full asset-tree rglob repeated for every material during analyze/process
    DESC: analyze_textures loops over materials (line 543) and for each one calls find_normal_candidate -> collect_image_files (line 377), which does folder.rglob('*') over the entire 0_source_mmd_assets tree (collect_image_files line 282), resolving every path. resolve_base_texture_path does the same recursive walk per material whenever the recorded path is missing (lines 352-359), and process_textures repeats it per row (line 686). With M materials and F
    FIX: Scan the search directories once per run (cache list[Path] plus the texture_name_keys index keyed by directory) and reuse it across all materials in both resolve_base_texture_path and find_normal_candidate.

[medium|perf|preview] model_preview.py:1786 _build_material_arrays performs a Path(...).exists() filesystem stat for every sampled triangle
    DESC: Inside the per-triangle loop (lines 1749-1790), lines 1786-1790 build a Path from triangle['texture_path'] (or the material's base_color_path) and call path.exists() for each triangle. The Blender-side generator (blender_sort_materials.py collect_material_preview, max_triangles=500000) can emit up to 500,000 sampled triangles, and the texture path is identical for all triangles of a material. That means up to 500k Path constructions plus 500k sta
    FIX: Resolve the texture path once per material uid, not per triangle: keep a dict cache keyed by the raw texture string (texture_raw -> Path | None) so exists() is called at most once per unique path, or compute texture_paths in a separate pass over self._materials after the triangle loop.

[medium|bug|preview] model_preview.py:495 OpenGL textures are never deleted - GPU memory leak on every model reload/rescan
    DESC: There is no glDeleteTextures call anywhere in the file (verified by grep). StaticModelPreviewWidget._ensure_textures (line 495) resets self._texture_ids = {} and generates fresh texture objects after each load_model/clear_model, abandoning the previous GL texture names. MaterialPreviewWidget does the same: set_material_data (line 1493) clears _material_texture_ids and _texture_id_for_uid (lines 2096-2106) creates new textures lazily. Character te
    FIX: Before clearing the id dicts, call self.makeCurrent(); GL.glDeleteTextures([ids]); self.doneCurrent() (guarding for an invalid context), and connect QOpenGLContext.aboutToBeDestroyed to a cleanup slot so reloads and widget teardown release GPU memory.

[medium|bug|preview] model_preview.py:458 StaticModelPreviewWidget latches _gl_error forever; one transient paint exception permanently hides the mesh
    DESC: In StaticModelPreviewWidget.paintGL, any exception during GL drawing sets self._gl_error (lines 458-459), and once set, every subsequent frame fills the entire widget with the 'OpenGL preview is unavailable in this context' message (lines 472-475), painting over the GL framebuffer even on frames where _draw_mesh_gl succeeded. _gl_error is only cleared in load_model/clear_model, so a single transient failure (e.g. context churn while docking/repar
    FIX: Mirror MaterialPreviewWidget: clear self._gl_error = "" after a successful _draw_mesh_gl call inside paintGL, and use a separate field (or just log) for overlay-painting errors so a one-frame failure does not permanently replace the rendered mesh.

[medium|perf|preview] model_preview.py:1226 SkeletonPlanPreviewWidget re-projects every bone multiple times per mouse-move and per paint
    DESC: _nearest_bone_at (lines 1226-1245) is called from _update_hovered_bone on every no-button mouse move (mouseMoveEvent line 1326-1328); it calls _projected_bone_heads(), which rebuilds a numpy array from the bone dicts, multiplies the full matrix stack, and allocates one QPointF per bone - then does two more O(n) passes (point distance + segment distance). When a bone is hovered, any >1px move triggers a full repaint (lines 1255-1256), and a single
    FIX: Cache the projected points dict on the instance, keyed by (view_yaw, view_pitch, view_zoom, view_pan, width, height, bones-generation counter), invalidate it in set_analysis/reset_front_view/mouse handlers that change the view, and have _nearest_bone_at and all _draw_* helpers reuse the cached dict. Keeping the projected coordinates as a single num

[medium|bug|proportion-release] blender_export_proportion_trick.py:851 Fresh-session verification can be masked by a stale report and a dead returncode check
    DESC: run_fresh_session_verification (lines 822-856) launches an inner Blender with --factory-startup --background --python (lines 828-835) but no --python-exit-code, so when the generated verifier script raises RuntimeError (PT constraints remain, meshes not retargeted, export failed, missing anims — lines 775-808 of the generated script), the inner Blender still exits 0 and the returncode check at line 849 never fires for Python errors. Failure detec
    FIX: Delete verify_report (and verify_dir) before launching the subprocess, and add "--python-exit-code", "1" to the inner Blender command so verifier exceptions produce a non-zero return code.

[medium|bug|proportion-release] blender_export_proportion_trick.py:405 enforce_single_physics_group mutates vertex group assignments while iterating live VertexGroupElement wrappers
    DESC: At lines 405-410, `for assignment in list(vertex.groups)` snapshots the collection, but each element is a live wrapper into the vertex's MDeformWeight array. Calling `other_group.remove([vertex.index])` inside the loop reallocates/shifts that array, so subsequent `assignment.group` reads go through stale pointers — a classic bpy pitfall that yields skipped or wrong removals (and can intermittently crash Blender). The practical effect is that phys
    FIX: Collect plain ints first: `other_indices = [g.group for g in vertex.groups if g.group != group.index and g.group in group_by_index]`, then loop over those indices calling group_by_index[i].remove([vertex.index]). No live wrapper is then read after a mutation.

[medium|perf|proportion-release] blender_export_proportion_trick.py:320 Zero-weight bone scan is O(groups x vertices) instead of one pass over vertices
    DESC: remove_zero_weight_nonessential_bones (lines 313-321) loops every vertex_group of every mesh and, per group, scans ALL mesh vertices calling vertex_group_weight() (which itself iterates vertex.groups). The any() generator short-circuits for weighted groups, but every zero-weight group — exactly the ones this feature targets, often dozens to hundreds on MMD rigs — forces a full scan of every vertex of every mesh. For a typical 100k-300k vertex ani
    FIX: Do a single pass per mesh: `for v in mesh.vertices: for g in v.groups: if g.weight > threshold: weighted.add(names_by_index[g.group])` where names_by_index maps obj.vertex_groups indices to names. That is O(total vertex-weight entries) once, regardless of group count.

[medium|bug|proportion-release] sort_release_description.py:370 cleanup_ai_text truncates everything after the first '[' character
    DESC: cleanup_ai_text (lines 367-370) ends with `return text.split("[")[0].strip()`, discarding the rest of the string at the FIRST literal '['. It is applied to the AI-generated description_en, quote_text, and quote_author (lines 320-321, 337-339). The OpenAI prompt itself (lines 292-295) uses square brackets as placeholder notation ('[character name] ([original native name if known]...'), so a model that echoes any bracketed fragment — or any legitim
    FIX: Replace the blanket split with targeted stripping: remove only trailing bracketed annotations (e.g. re.sub(r'\s*\[[^\]]*\]\s*$', '', text)) or strip known citation patterns, instead of cutting at the first '['.

[medium|perf|qc-compile] sort_qc_compile.py:1057 qc_analysis.json preview serialization: up to 500k triangles written with indent=2 and per-triangle texture_path
    DESC: build_preview() stores every preview triangle as a dict with 9 position floats, 6 UV floats, the material uid string, AND the full filesystem `texture_path` string repeated per triangle (lines 1057-1065), capped at MAX_PREVIEW_TRIANGLES=500000 (line 24). analyze() then writes this via write_json() which uses indent=2 (line 106), putting every float on its own line. For a typical 100k-300k triangle MMD model this produces a 50-300+ MB qc_analysis.
    FIX: Write the analysis JSON compactly (separators=(",", ":") or at least no indent for the model_preview subtree), drop the per-triangle texture_path (look it up via material_uid from the materials list), and consider flat float arrays instead of nested per-vertex lists. This typically shrinks the file 5-10x and cuts both write and re-parse time.

[medium|bug|qc-compile] sort_qc_compile.py:3014 ASCII staging dirs under %LOCALAPPDATA% are never cleaned up after success
    DESC: external_tool_root() (lines 177-189) creates per-path-hash staging dirs under %LOCALAPPDATA%/MMDCharacterImporter/external_tool_staging. package_addon_gma() (lines 3014-3034) copies the ENTIRE addon folder (compiled models + all VTF materials, easily hundreds of MB) plus the output .gma into a `gmad_<sha1>` scratch dir when the addon path is non-ASCII; convert_one_vtf() (lines 2393-2416) creates a `vtf_<sha1>` scratch dir per texture. These are o
    FIX: Wrap the staged work in try/finally and shutil.rmtree the scratch dir after copying the result back, or sweep external_tool_staging for stale entries (e.g., older than N days) at script start.

[medium|bug|qc-compile] sort_qc_compile.py:3202 Hitbox probe exit code ignored; zero hitboxes proceeds silently with no warning in report
    DESC: The studiomdl -h hitbox probe result is captured as `_code, hbox_output = run_command(...)` and the exit code is never checked. If the probe compile fails, extract_hboxes() returns [] and the final QCs are written without any $hbox lines; studiomdl then auto-generates hitboxes for every weighted bone (hundreds for MMD models, all in group 0), producing bad in-game hit detection. The only trace is the stdout line 'Captured 0 hitbox lines.' (line 3
    FIX: Check the probe exit code and `if not hboxes:` append a warning to `warnings` (e.g., 'Hitbox probe failed/produced no $hbox lines; the compiled model will use auto-generated hitboxes. See compile_hbox.log'), so it surfaces in qc_report.json and the GUI.

[medium|perf|vrd-carms] blender_sort_vrd.py:606 O(bones x vertices) full-scene rescan per bone during VRD inference
    DESC: infer_vrd_rows (line 600-607) calls collect_bone_weight_stats() once for EVERY non-essential bone in the armature. Each call (lines 490-531) iterates every vertex of every mesh object (with an inner loop over vertex.groups in weight_for_vertex, lines 483-487), iterates every polygon testing 'any(index in weighted_vertices for index in poly.vertices)' (lines 511-516), and rebuilds vertex_group_indices(obj) from scratch (line 497). For a typical MM
    FIX: Do a single pass per mesh object: walk obj.data.vertices once, and for each group reference above the threshold accumulate count/centroid/min/max into a dict keyed by group name; walk polygons once accumulating material_uids/face counts per group. Then score all bones from the precomputed dict. Also cache collect_bone_weight_stats results (e.g., fu

[medium|bug|vrd-carms] blender_sort_vrd.py:675 Center cloth bones export two <helper> blocks for the same procedural bone; Source honors only one procedural rule per bone
    DESC: For side == 'center' candidates, infer_vrd_rows emits two rows for the same procedural bone (drivers L_Thigh and R_Thigh, lines 669-675), and manual_export_vrd writes one <helper> block per enabled row (lines 1249-1271), producing two <helper> entries targeting the same bone (the L4D2 addon path does the same via paired cxbone/qdbone entries, create_vrd_project lines 1218-1225). In the compiled model each bone has a single procindex/proctype slot
    FIX: For center panels, either pick the dominant thigh (nearest by signed side distance) as the single driver, or document/warn that center bones can only follow one driver. If two-driver response is required, the bone needs to be split into L/R helper bones (one per driver) before export. At minimum add a validation warning when two enabled rows share 

[low|bug|bodygroups] blender_sort_bodygroups.py:2190 display_split_candidates renames candidates against an empty name set, allowing collisions with existing bodygroup names
    DESC: detect_advanced_split_candidates correctly seeds unique_name with all existing bodygroup names (line 2169), but display_split_candidates then re-runs unique_name over the selected subset with a fresh empty set (line 2190: used_names: set[str] = set()), undoing the de-duplication. A candidate base name like 'Hair_Tie_L' that was suffixed to 'Hair_Tie_L_02' reverts to 'Hair_Tie_L' and can collide with an existing bodygroup of that name. The collisi
    FIX: Seed used_names in display_split_candidates with {group['proposed_name'] for group in bodygroups} (pass bodygroups in, as detect_advanced_split_candidates does) before re-running unique_name.

[low|bug|bodygroups] blender_sort_bodygroups.py:499 Factor scale mode has no guard against zero/negative scale factor
    DESC: In maybe_scale_character, the preset path rejects non-positive computed scales (line 489-491), but the factor path (lines 495-499) applies float(scale_factor) directly with no check. A --scale-factor of 0 collapses the entire armature and all meshes to a point (and transform_apply bakes it in), and a negative value mirrors the model; both then propagate through pruning (all shapekeys deleted at scale 0) and produce a garbage output blend with no 
    FIX: Mirror the preset-path guard: if actual_scale <= 0 (or abs(actual_scale - 1.0) <= 1e-5), append a warning and return without scaling.

[low|build|bodygroups] blender_sort_bodygroups.py:136 Hardcoded developer drive paths for scale presets and an unreachable fallback warning
    DESC: SCALE_PRESET_FACE_SMDS points at E:/G/Upload/acheron|firefly|yaoyao_alt/5_propo/Face.smd (lines 136-140), paths that only exist on the developer's machine. On user machines resolve_scale_reference returns None and maybe_scale_character falls back to the baked SCALE_PRESET_FACE_TOPS constants (lines 478-479), so preset behavior silently depends on whether those E: files happen to exist (dev machine parses SMDs that may drift from the shipped const
    FIX: Remove SCALE_PRESET_FACE_SMDS (keep only the numeric SCALE_PRESET_FACE_TOPS constants plus the explicit --scale-reference-smd override) and delete the dead warning branch.

[low|bug|bodygroups] blender_sort_bodygroups.py:1091 collect_sources does not exclude Source/VTA helper meshes that validate-manual explicitly removes
    DESC: is_helper_mesh_object (lines 192-200, covering 'smd_bone_vis' and 'VTA vertices' meshes created by Blender Source Tools imports) is only applied in validate_manual_bodygroups (lines 3012-3016). collect_sources (line 1091) and separate_by_materials iterate all mesh objects, so if the analyze input blend contains such helpers (e.g. the user re-runs Step 6 analysis on a blend they touched in Blender after importing a reference SMD/VTA, via the GUI's
    FIX: Skip is_helper_mesh_object(obj) objects in collect_sources and separate_by_materials (or remove them up front in analyze_scene like validate_manual_bodygroups does, with a warning in the analysis report).

[low|bug|bones-spine] blender_sort_bones.py:1032 Weight transfer uses associated_meshes() but validation and weight totals scan ALL meshes, causing false hard failures
    DESC: batch_transfer_vertex_groups removes source vertex groups only from associated_meshes(armature) (lines 906, 948-952) — meshes with an armature modifier bound to this armature, falling back to all meshes only when none match (line 126). But validate_after_apply iterates mesh_objects() (all meshes, line 1032) and flags "Removed source vertex group remains on <mesh>" as a hard error, and before/after totals (vertex_group_weight_totals, lines 213-228
    FIX: Use the same mesh set everywhere: restrict vertex_group_weight_totals and the leftover-group check in validate_after_apply to associated_meshes(armature), or transfer/remove groups on all meshes.

[low|bug|bones-spine] blender_sort_bones.py:1078 Weight-conservation check ignores Blender's [0,1] clamp on vertex_group ADD and uses a tight absolute tolerance
    DESC: validate_after_apply requires `after_weights[target] + 0.0001 >= expected_total` where expected_total is the sum of pre-merge totals of all bones folded into the target. Blender clamps a vertex-group weight to 1.0 when target_group.add(..., "ADD") is applied (batch_transfer_vertex_groups line 945), so any vertex whose combined source+target weight exceeds 1.0 (non-normalized weights, which MMD imports can contain after repeated ADD-mode merges in
    FIX: Compute the expected total from the actual clamped per-vertex sums (min(1.0, source+target) per vertex) or downgrade this check to a warning, and use a relative tolerance.

[low|bug|bones-spine] blender_fix_spine_bones.py:1196 rebuild_armature_data captures hide/hide_select but never restores them; data swap drops bone custom properties and pose-bone data
    DESC: bone_data records "hide" and "hide_select" for every bone (lines 1181-1182), but the reconstruction loop (lines 1196-1209) only sets head, tail, roll, use_deform, parent and use_connect — the captured flags are silently discarded, so hidden bones become visible after the rebuild. Because the function replaces armature.data wholesale (line 1191), per-bone custom properties, bone collections, bendy-bone settings and pose-bone data (constraints, mmd
    FIX: Apply the captured hide/hide_select flags in the creation loop, and copy bone custom properties (bone.keys()) if any downstream step relies on them; otherwise remove the dead capture.

[low|bug|bones-spine] blender_sort_bones.py:762 ~130 lines of unreachable legacy merge path (merge_bone / addon_merge_bone / transfer_vertex_groups / delete_bone_internal)
    DESC: apply_plan exclusively uses the batched strategy (prepare_batched_operations + batch_transfer_vertex_groups + delete_bones_internal_batched, lines 1102-1132). merge_bone (line 762), addon_merge_bone (725), transfer_vertex_groups (672), delete_bone_internal (698), ensure_target_vertex_groups (666) and deselect_all_edit_bones (718) are never called from anywhere in the repo (verified by grep; the only other voyage_vrsns_merge_bones reference is ble
    FIX: Delete the unused per-bone merge path, or wire merge_bone in as an explicit fallback if the addon-based merge is still intended to be used.

[low|build|build-infra] build_mmd_character_importer_exe.ps1:206 Add-RequiredData bundles tools\__pycache__ (3.1 MB stale .pyc) and build scripts into the exe
    DESC: Add-RequiredData "tools" "tools" passes the whole tools directory to PyInstaller --add-data with no filtering. Verified on disk: tools\__pycache__ contains 21 files / 3.19 MB of cpython-312 .pyc artifacts that get embedded in every release exe (and extracted at each launch in onefile mode). It also ships the PowerShell build/sync scripts inside the end-user exe. Notably, sync_github_upload.ps1 already implements exactly this filtering (Test-Exclu
    FIX: Stage a filtered copy of tools/ (excluding __pycache__, *.pyc/*.pyo, and the *.ps1 build scripts) into a temp dir — the script already does this pattern for build_info.json — and pass that to --add-data; or delete __pycache__ dirs before invoking PyInstaller.

[low|build|build-infra] sync_github_upload.ps1:153 Sync script regenerates repo files from embedded here-strings that have already drifted from the live repo
    DESC: Lines 153-445 write requirements-build.txt, .gitignore, .gitattributes, build_assets_manifest.json, scripts/download_build_assets.ps1 and README.md from here-string copies duplicated inside this script, even though the repo root tracks the same files. Drift is already real: the generated .gitignore (lines 163-179) lacks the live .gitignore's entries (/9_arts, /Github Upload, /reference, /smoke_tests, both .spec names), and the generated README ("
    FIX: Make the tracked root files the single source of truth: have the sync script Copy-RequiredFile the existing requirements-build.txt, build_assets_manifest.json, scripts/download_build_assets.ps1 and a maintained docs/upload README instead of embedding their content as here-strings; keep only the upload-specific .gitignore generated.

[low|build|build-infra] en.json:1 en.json is an unreachable 300 KB duplicate of en_us.json maintained in parallel, with one conflicting value
    DESC: Scripted comparison of all 9 locale files shows every file has the identical 4222-key set (0 missing / 0 extra everywhere — translations are consistent). However en.json duplicates en_us.json byte-for-byte except for a single value: app.title is 'MMD Character Importer' in en.json vs 'Gmod Character Importer' in en_us.json. The GUI never loads 'en': LANGUAGE_OPTIONS (mmd_character_importer_gui.py lines 78-87) only offers en_us..ru_ru, the fallbac
    FIX: Delete tools/i18n/en.json (or turn it into the canonical file and make en_us a thin alias), and reconcile the app.title discrepancy first so the intended product name survives.

[low|bug|build-infra] sync_github_upload.ps1:20 -OutputDir parameter is dead: any value other than the default is always rejected
    DESC: The script advertises param([string]$OutputDir) (line 2), but Assert-SafeUploadRoot (line 20) throws unless the resolved value equals exactly '<projectRoot>\Github Upload' — the same path used when the parameter is omitted. So sync_github_upload.ps1 -OutputDir D:\anything (or even a differently-cased relative spelling that resolves elsewhere) always fails with 'Refusing to sync unexpected output folder', making the parameter unusable and misleadi
    FIX: Either remove the -OutputDir parameter entirely, or relax Assert-SafeUploadRoot to accept any folder whose leaf is 'Github Upload' (keeping the destructive Remove-Item guarded by the name/containment checks that already exist).

[low|build|build-infra] GmodSimpleMMDCharacterImporter.spec:8 Both .spec files reference deleted temp dirs and machine-specific paths; rebuilding from spec always fails
    DESC: GmodSimpleMMDCharacterImporter.spec line 8 and MMDCharacterImporter.spec line 8 embed datas entries pointing at C:\Users\1peng\AppData\Local\Temp\mci_build_info_<guid>\build_info.json — a directory the build script deliberately deletes in its finally block (build_mmd_character_importer_exe.ps1 lines 515-521) — plus hardcoded C:\ProgramData\miniforge3 DLL paths and absolute project paths. Running 'pyinstaller GmodSimpleMMDCharacterImporter.spec' t
    FIX: Delete both stale .spec files from the working tree (the supported build path is tools/build_mmd_character_importer_exe.ps1, which regenerates a spec each run); if a checked-in spec is ever desired, generate it with relative paths and a runtime-created build_info hook.

[low|bug|build-infra] build_assets_manifest.json:10 Manifest 'required' flag is never consumed and 'reason' contradicts the build script's optional handling
    DESC: scripts/download_build_assets.ps1 iterates $Manifest.assets and downloads every entry unconditionally — the "required": true field (line 9) is never read, so marking a future asset optional would have no effect. The "reason" text (line 10) states the Blender zip is 'required by tools/build_mmd_character_importer_exe.ps1', but that script treats it as optional via Add-DataIfExists (line 209) and its own release notes say the app auto-downloads Ble
    FIX: Either honor the required flag in download_build_assets.ps1 (skip non-required assets unless -All, and only fail -VerifyOnly for required ones) or drop the field; reword the reason to 'optional offline-fallback Blender zip; bundled into the exe when present, otherwise auto-downloaded at first run'.

[low|bug|build-infra] build_mmd_character_importer_exe.ps1:489 dependency_manifest.json written with Set-Content -Encoding UTF8 gets a BOM under Windows PowerShell 5.1
    DESC: Line 489 ($Manifest | ConvertTo-Json | Set-Content -Encoding UTF8) and line 501 (RUN_ME.txt) emit UTF-8 with BOM when the script is run under Windows PowerShell 5.1 — which is what the project README's documented invocation ('powershell -ExecutionPolicy Bypass -File ...') uses. A BOM-prefixed JSON file breaks strict JSON parsers (Python json.load on bytes, jq, many CI tools) that may later consume the dependency manifest. The script itself is inc
    FIX: Write the manifest and RUN_ME.txt with the same BOM-less [System.IO.File]::WriteAllText(..., UTF8Encoding($false)) helper used for build_info.json, or require pwsh 7 where -Encoding UTF8 is BOM-less.

[low|bug|collision] blender_sort_collision.py:2852 validate_plan hard-fails on disabled parts, making the per-part 'enabled' flag unusable
    DESC: Plan rows carry an 'enabled' flag and create_physics_object (lines 2984-2985) skips disabled parts when building the Physics mesh, but validate_plan (lines 2852-2853) appends an error ('Collision part for X is disabled') for any disabled target, and apply_scene raises before creating anything when validation has errors (line 3224). validate_physics_object additionally errors if any target bone has no weighted vertices (lines 3049-3051). So settin
    FIX: Either remove the 'enabled' field from the schema and create_physics_object, or make validate_plan/validate_physics_object treat disabled default-target parts as warnings and skip the per-bone geometry checks for them.

[low|perf|collision] blender_debug_select_collision_vertices.py:304 Debug selection tool performs 4+ full-scene scans and one vertex_groups.add RNA call per vertex weight
    DESC: compute_fit (lines 115-122) builds both the filtered and unfiltered weighted vertex clouds (two full passes over every vertex x ~18 targets via collect_weighted_vertex_cloud), compute_selected_source_vertices (lines 178-189) then recomputes target and per-child weights vertex-by-vertex with vertex_group_weight_any (which rebuilds a set per call), plus weight_threshold_for_bone per child does yet another full pass. Finally create_merged_debug_obje
    FIX: Reuse blender_sort_collision.build_collision_source_index for both fit and selection (one pass, indexed weights), and batch vertex-group assignment by grouping indices with equal weight into single add() calls (or assign via foreach_set on a temporary attribute).

[low|perf|core] mmd_character_importer_core.py:2639 import_pmx_to_blender re-parses the whole PMX even when an analysis already exists
    DESC: import_pmx_to_blender unconditionally calls analyze_pmx (line 2639), which parses every vertex/face/material/bone/morph in pure Python with one handle.read per field (up to 300k vertices = millions of read calls) plus a texture rglob, and build_workspace additionally does a full file_sha1 of the PMX (line 2240). The GUI always runs analyze_pmx for the preflight first (gui lines 8950, 10422) and then starts the import worker, so the file is fully 
    FIX: Add an optional analysis: PmxAnalysis | None parameter to import_pmx_to_blender and pass the GUI's preflight result through, falling back to analyze_pmx only when absent.

[low|perf|core] mmd_character_importer_core.py:2613 copy_asset_tree duplicates the entire selected folder with no size guard
    DESC: copy_asset_tree rglob-copies every file under the user-selected source dir into the workspace (lines 2609-2629), and find_pmx_files (line 4796) deliberately searches recursively, so the GUI encourages selecting a parent folder containing several models. A user pointing at their MMD collection root silently duplicates many GB into %LOCALAPPDATA% on every import (plus the analyze-phase texture rglob over the same tree), with progress reported only 
    FIX: Pre-compute the byte total before copying and warn/confirm above a threshold (e.g. 2 GB), report incremental progress during the copy, and consider restricting the copy to the PMX's own subfolder plus referenced texture paths.

[low|bug|core] mmd_character_importer_core.py:2247 Unhelpful ValueError when PMX is not under the given source dir (CLI)
    DESC: build_workspace computes rel_pmx = pmx_path.relative_to(source_dir) (line 2247). The GUI constrains the PMX combo to files found under the selected folder, but the CLI 'import' command accepts an arbitrary --source-dir (line 4818); if it is not an ancestor of the PMX, the user gets a raw ValueError "... is not in the subpath of ..." instead of a clear message, after analyze_pmx already spent time parsing the file.
    FIX: Validate early in import_pmx_to_blender/build_workspace: if pmx_path is not under source_dir, raise a RuntimeError explaining that the PMX must live inside the selected model folder.

[low|bug|core] mmd_character_importer_core.py:4510 Icon processor output is discarded when a Blender render precedes it
    DESC: In run_icons, the second subprocess (sort_icons_and_arts.py process mode) is run with processor_log = log_path only when a custom source image is used; in the normal render path it is None (line 4510), so the processor's output is never written to disk. Worse, when it IS logged it reuses the same log_path that the render just wrote, and run_process_streamed opens logs with mode 'w' (line 1217), so each phase overwrites the previous one - icon ana
    FIX: Give each phase its own log file (e.g. icons_analyze.log, icons_render.log, icons_process.log) or open the shared log in append mode with a phase header.

[low|bug|flexes-import] blender_sort_flexes.py:267 CATS shapekey prune can never run: process uses --factory-startup and the script never enables CATS
    DESC: prune_shapekeys (lines 254-275) tries bpy.ops.cats_shapekey.shape_key_prune() first, but the core launches this script with --factory-startup (core 3415-3430) and, unlike blender_import_mmd_model.py/blender_fix_mmd_model.py, blender_sort_flexes.py has no enable_cats() call. The CATS operator is therefore never registered in this process: every mesh takes the exception path, falls back to fallback_prune_shapekeys, and appends a spurious 'CATS shap
    FIX: Either add the same enable_cats() bootstrap used by steps 1-2 before calling the CATS prune operator, or drop the CATS attempt entirely and use fallback_prune_shapekeys directly (renaming its method tag), removing the per-object 'unavailable' warnings.

[low|bug|flexes-import] blender_fix_mmd_model.py:197 Custom-normal data fallback is a silent no-op, yet the mesh is still counted as cleared
    DESC: When the edit-mode operator path in clear_custom_normals fails (lines 213-222), the fallback clear_mesh_custom_normals_data calls mesh.normals_split_custom_set(None) (line 197). In Blender 4.x this raises (the RNA function requires a sequence of per-loop normals; None is rejected), the exception is swallowed by the bare `except Exception: pass`, and nothing is cleared. Execution then continues to `cleared += 1` (line 223), so the report fields cu
    FIX: Implement a working data-level fallback for Blender 4.x (remove the 'custom_normal' attribute: `attr = mesh.attributes.get("custom_normal"); mesh.attributes.remove(attr)` when present), and only increment `cleared` after re-checking `mesh.has_custom_normals` is False; otherwise record a warning in the report.

[low|bug|flexes-import] blender_sort_flexes.py:122 load_reference_mapping swallows all errors, silently degrading flex naming to the 21-entry fallback
    DESC: load_reference_mapping (lines 109-124) parses the flex-name dictionary out of reference/li_zhiyan_npc/3_Flexes/Blender_p3.py via AST, with a bare `except Exception: pass`. If that file is missing, moved, or its `special_replacement_dict` assignment changes shape (it is a reference asset for a specific sample NPC, listed separately in build_mmd_character_importer_exe.ps1:220 — easy to drop when packaging changes), the script silently falls back to
    FIX: Record the load failure: catch the exception, append a warning into the analysis output (e.g. a top-level 'reference flex mapping unavailable: <reason>' warning in analyze_scene) so degraded naming is diagnosable, and consider embedding the full mapping as data (JSON next to the script) instead of AST-parsing a sample NPC's script at runtime.

[low|bug|gui-helpers-workers] mmd_character_importer_gui.py:908 Cancelling a manual-edit Blender session reports a bogus 'Blender exited with code 1' error
    DESC: SortBonesManualMergeWorker.cancel() (lines 887-890) terminates the Blender process from the GUI thread, but the run() poll loop (lines 908-914) checks process.poll() BEFORE checking self.cancel_requested. After cancel's terminate() succeeds, the next poll returns exit code 1, the loop breaks, and line 915-916 raises 'Blender exited with code 1 during manual bone merging.' instead of the cancellation message — so a user-initiated cancel is surface
    FIX: In the run() loop, check self.cancel_requested before/along with the poll result: after breaking, if self.cancel_requested raise the cancellation RuntimeError regardless of return_code. Guard cancel() bodies: capture proc = self.process into a local, then 'if proc and proc.poll() is None: try: proc.terminate() except Exception: pass'.

[low|bug|gui-helpers-workers] mmd_character_importer_gui.py:614 PathRow.value() does not strip surrounding quotes, so Windows 'Copy as path' input breaks all rows except the GMod row
    DESC: PathRow.value() (lines 614-615) returns self.edit.text().strip() without removing surrounding quotes. The module already has clean_path_text() (lines 120-124) exactly for this, but it is only applied to the GMod/studiomdl row via resolve_gmod_path_input (line 135). Windows 11's right-click 'Copy as path' wraps paths in double quotes; pasting such a path into the MMD source folder, distribution output, workspace, VMD, or custom icon image rows pas
    FIX: Return clean_path_text(self.edit.text()) from PathRow.value() (and optionally normalize on paste/editingFinished), so every path field tolerates quoted input the same way the GMod row does.

[low|bug|gui-helpers-workers] mmd_character_importer_gui.py:94 Developer-machine path E:/G/Upload hardcoded as default category scan root
    DESC: DEFAULT_CATEGORY_SCAN_ROOTS = [Path("E:/G/Upload")] (line 94) is used by load_category_suggestion_config (line 480) whenever no config file or env var provides scan_roots — which is the out-of-box state for every end user. scan_known_category_suggestions (line 3063) then checks root.exists() and, if a user happens to have an E:\G\Upload folder (common drive letter for external disks), silently walks it for up to 4 seconds and reads lua/autorun/*.
    FIX: Default DEFAULT_CATEGORY_SCAN_ROOTS to an empty list (or to the detected GMod addons folder) and keep E:/G/Upload only in the developer's local category_suggestions.json under %LOCALAPPDATA%.

[low|bug|gui-helpers-workers] mmd_character_importer_gui.py:205 studiomdl path resolution assumes <root>/bin/studiomdl.exe layout; bin/win64 (GMod x86-64 branch) is not handled
    DESC: resolve_gmod_path_input only probes path/'bin'/'studiomdl.exe' (line 151) and path/'studiomdl.exe' (line 161); it never checks bin/win64/studiomdl.exe, which exists on Garry's Mod's x86-64 beta branch. Worse, gmod_game_dir_from_path_input for kind 'studiomdl' computes game_dir = studiomdl.parent.parent / 'garrysmod' (lines 205-206): if a user explicitly browses to .../GarrysMod/bin/win64/studiomdl.exe, parent.parent is .../GarrysMod/bin, so the c
    FIX: In resolve_gmod_path_input, also probe bin/win64/studiomdl.exe for install roots; in gmod_game_dir_from_path_input, walk up from the selected studiomdl.exe until a sibling 'garrysmod' folder with gameinfo.txt is found instead of assuming exactly two parent levels.

[low|bug|gui-helpers-workers] mmd_character_importer_gui.py:544 gui_crash.log grows without bound (append mode, no rotation)
    DESC: enable_crash_logging (lines 544-567) opens %LOCALAPPDATA%/MMDCharacterImporter/logs/gui_crash.log in append mode at every launch (called at line 19565), writes a start marker, and registers faulthandler plus sys.excepthook against the same handle. There is no rotation, truncation, or size cap, so on a machine that hits recurring exceptions the file grows indefinitely; the handle is also never closed (only mitigated by process exit).
    FIX: Rotate the log at startup (e.g. rename to gui_crash.log.1 when over ~5 MB, or keep N timestamped files and prune old ones) before opening the append handle.

[low|perf|gui-helpers-workers] mmd_character_importer_gui.py:271 Directory-size scans follow Windows junctions, risking cycles and double counting
    DESC: folder_size_and_mtime (lines 256-281) and WorkspaceSizeWorker.scan_workspace_root (lines 2535-2566) recurse using entry.is_dir(follow_symlinks=False), which returns True for Windows directory junctions (junctions are not symlinks to os.DirEntry). A junction inside an addon or workspace that points to an ancestor causes the scan to loop until path length errors finally raise OSError, and junctions pointing at large external trees inflate the repor
    FIX: Skip reparse-point directories during recursion: on Python 3.12 use Path(entry.path).is_junction() (or check entry.stat(follow_symlinks=False).st_reparse_tag) and treat them like symlinks (counted as zero-size leaf entries).

[low|perf|gui-window-A] mmd_character_importer_gui.py:2922 Constructor runs a recursive Lua autorun disk scan (up to 4 s per scan root plus file reads) before the window appears
    DESC: ImporterWindow.__init__ calls scan_known_category_suggestions() synchronously (line 2922) before any widget is shown. iter_lua_autorun_files (3039-3061) walks each scan root to depth 6 with a max_seconds=4.0 budget per root, and the outer loop (3063-3084) reads the full text of every .lua file found; the 4-second budget only caps the directory traversal between yields, not the read_text + regex work at 3066-3077, so total startup blocking can exc
    FIX: Run the category scan in a QThread worker after the window shows and call install_category_completers when results arrive (the completers are only needed once the user focuses the category fields), or cache scan results in QSettings keyed by root mtime. Drop the hardcoded E:/G/Upload default scan root.

[low|bug|gui-window-A] mmd_character_importer_gui.py:5611 material_always_merge / flex_isolate_bodygroup checkboxes and CoACD quality are never persisted on change (and there is no closeEvent fallback)
    DESC: Settings persistence is purely signal-driven (save_settings hooked to specific widgets) and ImporterWindow defines no closeEvent (verified: no 'def closeEvent' in the file). material_always_merge_check (created 5611) is loaded from QSettings at 8441 and written at 8517, but its toggled signal is never connected to save_settings (connections block 5915-5929) — unlike the sibling bodygroup_always_auto_split_check which does connect (line 6152). Sam
    FIX: Connect material_always_merge_check.toggled and flex_isolate_bodygroup_check.toggled to save_settings (matching bodygroup_always_auto_split_check at line 6152), add load/save for collision_quality_combo, and add a closeEvent override that calls save_settings() as a catch-all.

[low|bug|gui-window-A] mmd_character_importer_gui.py:3023 apply_startup_geometry forces a 580 px minimum height that exceeds short screens
    DESC: target_height = min(720, max(580, available.height() - 90)) floors the height at 580 even when the available desktop is shorter (e.g. 1024x600 netbook/projector: available ~560 px -> window and minimum size become 580 px tall). setMinimumSize(min(900, target_width), min(580, target_height)) then locks 580 as the minimum, so the bottom of the window (status bar, action buttons on unscrolled tabs) is permanently clipped below the taskbar and the us
    FIX: Clamp to the available geometry: target_height = min(720, max(min(580, available.height() - 90), available.height() - 90)) — i.e. use min(580, available.height() - 90) as the floor — and pass the same clamped values to setMinimumSize.

[low|bug|gui-window-B] mmd_character_importer_gui.py:8349 Saved icon render frame 0 is silently reset to 334 on restart
    DESC: Line 8349: `icon_frame = int(self.settings_store.value("icon_frame", 334, int) or 334)`. icon_frame_spin allows 0 (setRange(0, 999999), line 7386), and frame 0 is a legitimate choice for the icon render (first pose of the VMD). Because 0 is falsy, the `or 334` clause replaces a saved value of 0 with 334 on every load, so a user who sets the render frame to 0 gets 334 back after restarting the app.
    FIX: Use an explicit None/exception check instead of `or`: read the value, and only fall back to 334 when conversion fails (e.g., wrap int(...) in try/except and drop the `or 334`).

[low|bug|gui-window-B] mmd_character_importer_gui.py:7960 Skirt jigglebone preset has duplicated 'skirt' keyword and lacks Japanese skirt names
    DESC: The Skirt quick-filter preset passes the keyword list "skirt, skirt_, skirt, 裙" (line 7960) — 'skirt' appears twice, which strongly suggests one entry was meant to be something else. The Left/Right presets (7962-7963) include Japanese characters (左/右) because MMD bone names commonly survive in Japanese at this stage, yet the skirt preset only covers English and Chinese (裙), omitting the standard Japanese skirt bone naming スカート/スカ. Skirt bones on 
    FIX: Replace the duplicate entry with the Japanese keyword(s): e.g. "skirt, skirt_, スカート, スカ, 裙".

[low|bug|gui-window-B] mmd_character_importer_gui.py:9037 find_gmod_candidate ships developer-specific hardcoded fallback paths (H:/SteamLibrary, GarrysMod_RTX_c)
    DESC: find_gmod_candidate (9020-9052) falls back to hardcoded paths from the maintainer's machine: H:/SteamLibrary/steamapps/common/GarrysMod_RTX_c and H:/SteamLibrary/steamapps/common/GarrysMod (lines 9037-9041). On a user machine that happens to have an H: drive with a stale or foreign GarrysMod folder containing bin/studiomdl.exe (e.g., a copied backup), auto-detect would silently select it over nothing, and the GarrysMod_RTX_c name is a personal cu
    FIX: Drop the H:/ entries (keep only the standard 'C:/Program Files (x86)/Steam/...' fallback), or read extra fallbacks from an optional user-configurable setting.

[low|bug|gui-window-C] mmd_character_importer_gui.py:13121 split_bodygroup_vertex_group vertex_count lookup is dead code — split rows always show 0 vertices
    DESC: Lines 13120-13123 try to find the vertex count: `for related in entry.get("related_vertex_groups", []): if isinstance(related, dict) and related.get("name") == vertex_group`. But in plan bodygroup entries, related_vertex_groups is always a list of plain strings — blender_sort_bodygroups.py builds it via `str(group["name"]) for group ...` (lines 1390-1394) and cluster_tracking_groups returns list[str] (1664-1670); the GUI itself treats it as strin
    FIX: Drop the dict probing and look up the vertex-group count from the analysis sources (which carry dict entries with name/vertex_count), or show 'unknown' instead of a misleading 0.

[low|perf|gui-window-C] mmd_character_importer_gui.py:11140 Full .blend file copied synchronously on the GUI thread after every Step 4 analysis
    DESC: sort_analyze_done (a done-signal handler running on the main thread) calls prepare_sort_manual_merge_blend (11123 -> 11130), which executes shutil.copy2(input_blend, manual_blend) at 11140 to create the manual-merge checkpoint. Intermediate .blend checkpoints for MMD character models are routinely 100-500 MB, so the UI freezes for several seconds right at the moment the user expects the analysis results to appear, with the progress label already 
    FIX: Perform the copy inside the SortBonesAnalyzeWorker thread (it already produced input_blend), or defer creating the manual-merge copy until the user actually clicks the Manual Merge button.

[low|perf|gui-window-C] mmd_character_importer_gui.py:12072 Hover texture preview decodes full-resolution images on every row change with no cache or scaled decode
    DESC: load_texture_pixmap (12071-12086) reads the entire image at native resolution (QImageReader without setScaledSize, PIL fallback converts the whole image to RGBA), and show_material_texture_preview (12120-12132) then scales it down to ~220x160. It is invoked from mouse-move hover handlers (update_material_texture_preview_for_row 12017, merge variant 12044) every time the hovered row changes. MMD base-color textures are commonly 2048-4096 px (16-64
    FIX: Call reader.setScaledSize() to decode at preview resolution, and keep a small QPixmapCache/dict keyed by base_color_key so each texture is decoded once per session.

[low|bug|gui-window-C] mmd_character_importer_gui.py:11251 refresh_sort_bone_combos hasattr guard is dead — attribute is dereferenced before the check
    DESC: Lines 11250-11252: `for combo in (self.sort_protect_combo, self.sort_source_combo, self.sort_target_combo): if not hasattr(self, "sort_protect_combo"): return`. The tuple in the for-statement evaluates self.sort_protect_combo before the hasattr guard ever runs, so if the combos did not exist the method would raise AttributeError, not return. Today the call order is safe (combos created at 5465, the sort_input_row.changed connection made later at 
    FIX: Move the hasattr check above the for statement: `if not hasattr(self, 'sort_protect_combo'): return` before building the tuple.

[low|perf|gui-window-C] mmd_character_importer_gui.py:10575 save_settings (dozens of QSettings registry writes) runs on every keystroke in step input rows
    DESC: Every on_*_input_changed handler ends with self.save_settings() (10575, 10683, 11029, 11613, 12633, 13410) and these handlers fire per keystroke because PathRow.changed is connected to textChanged (line 612). save_settings (8480+) unconditionally writes a long list of QSettings values — on Windows each setValue is a registry write — plus the handlers also clear analysis state and run path-existence/preview updates (update_*_output_preview) on eac
    FIX: Connect PathRow.changed to editingFinished for typed input or debounce save_settings with a single-shot QTimer (e.g. 500 ms), keeping the immediate save only for browse/set_value paths.

[low|bug|gui-window-D] mmd_character_importer_gui.py:13920 Live plan dicts passed to FlexApplyWorker/CollisionApplyWorker without copy while tables stay editable
    DESC: start_flex_apply does `self.worker = FlexApplyWorker(str(input_blend), self.current_flex_plan)` (line 13920) and start_collision_apply does `CollisionApplyWorker(str(input_blend), self.current_collision_plan)` (line 15013). The worker thread serializes that same dict via json.dumps in core.sort_flexes_blend (core line 3480) / core.sort_collision_blend (core line 3750). Neither start method disables the flex/collision QTableWidget (no flex_table.s
    FIX: Pass deepcopy(self.current_flex_plan) / deepcopy(self.current_collision_plan) to the workers like the VRD step does, and/or disable the flex/collision tables while the apply worker runs.

[low|bug|gui-window-D] mmd_character_importer_gui.py:13739 Invalid rest/max flex values silently reset in the plan but the bad text stays visible in the table
    DESC: In on_flex_table_item_changed, columns 6 (rest_value) and 7 (max_amplitude) parse the cell text with float(); on failure they silently set the entry to 0.0 / 1.0 (lines 13739-13748) but never rewrite the cell text, so the table keeps displaying the invalid string (e.g. '0,5' from a European-locale user or a typo) while the plan holds a different value. The same handler fixes this correctly for column 1 (final_name resets the cell at 13735-13738),
    FIX: After the except branch for columns 6/7, set _updating_flex_table and item.setText(f"{entry['rest_value']:.3f}") (or the max value) so the cell always reflects the value actually stored in the plan.

[low|bug|gui-window-D] mmd_character_importer_gui.py:13563 Table-population guard flags not exception-safe; one malformed plan value permanently disables table editing
    DESC: populate_flex_table sets self._updating_flex_table = True at line 13563 and only resets it at line 13607 with no try/finally. Formatting expressions in between can raise on malformed plan/report data: flex_float (line 13366) raises ValueError for non-numeric strings and is called unguarded at lines 13591-13593; populate_collision_table similarly calls int(sampled) unguarded at line 14743 ('sampled_faces' from the CoACD dict) and sets _updating_co
    FIX: Wrap the population loops in try/finally that restores the _updating_* flag (copying the populate_vrd_table pattern), and make flex_float catch ValueError and return the default.

[low|bug|gui-window-E] mmd_character_importer_gui.py:16938 Live plan dicts handed to worker threads without copy while tables stay editable
    DESC: start_texture_process (line 16938) passes self.current_texture_plan directly to TextureProcessWorker, and start_qc_compile (line 18785) passes self.current_qc_plan (update_qc_plan_from_gui returns the same object) to QcCompileWorker. The workers store the reference (gui.py:1916, 2115) and core.process_textures / core.compile_and_compose_qc json.dumps the dict on the worker thread (core.py:4307, 4615). During the run only the action buttons are di
    FIX: Pass deepcopy(plan) into TextureProcessWorker and QcCompileWorker (as regenerate_vrd_preview does), and/or setEnabled(False) on the editing tables while the worker runs.

[low|bug|gui-window-E] mmd_character_importer_gui.py:16984 Step 12 shows green 'Complete' progress and success popup even when textures had errors
    DESC: texture_process_done sets the progress bar to 'Step 12 Complete' in green (#2ea043) at line 16972 BEFORE checking error_count, and the QMessageBox.information at line 16984 ('Step 12 complete / Texture PNG processing completed.') is shown unconditionally — including the errors > 0 branch where the step was just marked failed via set_step_failed at 16983. The user sees a green bar and a success dialog while the workflow state says failed, and the 
    FIX: Move the set_texture_progress and message box into the errors <= 0 branch; on errors show a warning box ('completed with N errors, see log') and amber progress color like qc_compile_done does.

[low|perf|gui-window-E] mmd_character_importer_gui.py:17350 Row-hover texture preview decodes full-resolution images synchronously with no cache
    DESC: texture_table has mouse tracking enabled (creation at 7256), so update_texture_hover (17292) fires per row as the mouse moves; it calls show_texture_preview (17316) → set_preview_pixmap (17350-17361), which constructs a full QPixmap from disk for BOTH the base texture and the normal map on every hover event, then scales them down. For typical 2K-4K MMD textures that is tens of MB decoded per row crossed, all on the GUI thread, producing visible s
    FIX: Cache scaled pixmaps keyed by (path, mtime) in a small dict or QPixmapCache, and/or use QImageReader.setScaledSize to decode at preview size.

[low|bug|gui-window-E] mmd_character_importer_gui.py:19325 'Open folder' slots silently create directories from arbitrary field text and can raise unhandled OSError
    DESC: open_workspace and the 15 copy-pasted open_*_folder slots (19325-19458) do path.mkdir(parents=True, exist_ok=True) on whatever string is currently in the corresponding output QLineEdit before opening Explorer. A stale path from a previous project, or a mistyped/edited path, silently creates a whole directory tree on click. If the text is an invalid Windows path (illegal characters like '|', nonexistent drive letter), mkdir raises OSError that is 
    FIX: Wrap in try/except and show_error on failure, and only mkdir when the path is a descendant of the known workspace root — otherwise just show 'folder does not exist'. Factor the 16 duplicates into one helper taking the QLineEdit.

[low|bug|gui-window-E] mmd_character_importer_gui.py:17383 icon_face_vmd_paths splits on ';' — breaks legal Windows paths containing semicolons
    DESC: icon_face_vmd_paths (17377-17383) splits the single face-VMD field on ';' (re.split(r";|\n", raw)). Semicolons are legal characters in Windows file and folder names (and appear in some MMD motion pack folder names, e.g. translated Japanese titles); such a path gets cut into two bogus fragments, and start_icon_generate then fails with 'Facial/flex VMD not found' for a path that exists (17599-17601). There is no escaping mechanism documented in the
    FIX: Prefer a QListWidget/multi-select file picker storing a real list, or only treat ';' as separator when both sides parse as existing paths; newline-only separation is already supported and unambiguous.

[low|build|icons-addons] sort_icons_and_arts.py:301 Hardcoded developer machine path baked into find_vtfcmd()
    DESC: find_vtfcmd() includes the literal path r"C:\Users\1peng\Modding\Plugins\vtflib132-bin\bin\x64\VTFCmd.exe" (line 301) in the shipped fallback list, ahead of the standard Program Files VTFEdit locations. On end-user machines it simply never matches, but it leaks the developer's personal directory layout into the public GitHub repo/release, and on the dev machine it can silently select a different (older) VTFCmd build than the bundled one if the bu
    FIX: Remove the personal path from the candidate list (the env var VTFCMD already covers local-dev overrides), keeping only the bundled copy, PATH lookup, and standard VTFEdit install locations.

[low|bug|icons-addons] blender_setup_addons.py:292 Manual zip extraction joins archive member names onto the target without sanitization (zip-slip / absolute-path escape)
    DESC: All four manual extraction loops -- install_mmd_tools_from_cats (line 292: `destination = target / rel`), install_material_combiner (line 349), install_coacd (line 396), install_l4d2_tools (line 425) -- write `target / rel` where rel comes straight from archive.namelist(). A member name containing '..' segments escapes the addons directory, and an absolute member name (e.g. 'C:/...') makes pathlib's joinpath discard `target` entirely, giving an a
    FIX: Normalize and validate each member before writing: resolved = (target / rel).resolve(); reject (or skip with a warning) when not resolved.is_relative_to(target.resolve()), and reject absolute member names / drive letters. Alternatively use zipfile.ZipFile.extract(), which performs this sanitization itself.

[low|bug|icons-addons] sort_icons_and_arts.py:441 Step 13 F.vmt/E.vmt reference a $basetexture name that never matches the VTF written beside them
    DESC: write_vmt() emits F.vmt with `$basetexture "vgui/entities/{basename}_F"` (lines 441-442) while the texture produced in the same folder is named F.vtf (line 454 via convert_to_vtf). A VMT/VTF pair is only usable when the basetexture path matches the VTF's installed name; as written, copying the step-13 output folder's F.vmt + F.vtf into materials/vgui/entities can never resolve (the VMT looks for {basename}_F.vtf, the file is F.vtf). Step 14 (sort
    FIX: Either name the step-13 outputs consistently ({basename}_F.vtf + {basename}_F.vmt so the pair works standalone), or stop emitting VMTs in step 13 since step 14 authoritatively regenerates them with the model_author naming.

[low|bug|materials-textures] blender_sort_materials.py:1590 materials.npy is always written with empty material names (final_name key never exists)
    DESC: save_material_mapping writes npy rows as (entry.get('final_name',''), entry.get('base_color_path','')) (lines 1588-1591), but mapping['final_materials'] is after_analysis['materials'] (lines 1635 and 1950), whose entries are produced by collect_material_entries (lines 567-583) and contain 'material_name'/'proposed_name' but never 'final_name'. So every materials.npy row has an empty name. sort_param_textures.read_materials_npy then falls back to 
    FIX: Write entry.get('proposed_name') or entry.get('material_name') into the npy name column (or add final_name to the after-analysis entries before saving).

[low|bug|materials-textures] sort_param_textures.py:126 Step 12 fails with FileNotFoundError when materials.npy is absent even though materials.json exists
    DESC: resolve_material_mapping only collects materials.npy candidates (lines 107-124) and raises FileNotFoundError at line 126 if none exist - even when a perfectly valid materials.json is present (including the case at lines 110-112 where the user explicitly selected materials.json but the sibling npy is missing). Yet read order prefers JSON and only uses npy as fallback (load_material_entries lines 193-195). The npy write in step 5 is best-effort: bl
    FIX: In resolve_material_mapping, also accept a materials.json found at the same locations: if json exists but npy does not, return (json-derived paths, npy_path=None) and skip the npy fallback in load_material_entries.

[low|bug|materials-textures] sort_param_textures.py:769 Enabled normal with vanished source reports its action name as status with no file and no warning
    DESC: In process_textures, when use_normal=True but the normal source no longer exists and default_normal_action is 'copy_blue'/'ambiguous_copy'/'convert_ue_rg', the elif chain (lines 746-761 all require normal_source.exists()) falls through to the final else (line 769), which sets row_report['normal_status'] = default_normal_action (e.g. 'copy_blue') while normal_output_path stays empty, no file is written, and no warning is appended. The manifest the
    FIX: Add an explicit branch: if use_normal and default action expects a source but normal_source is missing, set normal_status='failed' (or regenerate from base) and append a 'Normal source file is missing' warning; remove the dead inner existence checks.

[low|bug|preview] model_preview.py:2007 Material alpha of exactly 0.0 renders fully opaque due to 'or 1.0' fallback
    DESC: _material_rgba computes alpha = float(entry.get("alpha", values[3]) or 1.0). Because 0.0 is falsy, a material whose plan/scan entry has alpha == 0.0 (fully transparent - common in MMD models for toggleable parts) gets alpha 1.0 and is drawn fully opaque when 'respect alpha' is enabled (_respect_alpha). The very next branch (elif alpha <= 0.02: alpha = 0.04, lines 2010-2011) shows the intent was to render near-zero alpha as a faint ghost, but that
    FIX: Replace the falsy-coalescing with an explicit None check, e.g.: raw_alpha = entry.get("alpha"); alpha = values[3] if raw_alpha is None else float(raw_alpha). Then the existing <= 0.02 clamp handles fully transparent materials as intended.

[low|perf|preview] model_preview.py:492 All texture images are decoded and uploaded synchronously inside paintGL, freezing the first frame
    DESC: StaticModelPreviewWidget._ensure_textures (lines 492-501) runs during the first paintGL after a model load and synchronously decodes every material texture via QtGui.QImage(str(path)) plus convertToFormat (lines 503-507) before uploading. MMD characters routinely ship 10-30 PNG/TGA textures at 1-4K resolution; decoding them all in the paint handler blocks the GUI thread for a second or more on the first frame, on top of the parse freeze. Material
    FIX: Decode QImages on a worker thread (QImage decoding is safe off the GUI thread) when the model/scan is set, then only do the cheap glTexImage2D upload in paintGL; alternatively upload at most one or two pending textures per frame and request another update() so the UI stays responsive while textures stream in.

[low|perf|preview] model_preview.py:130 _texture_files recursively scans the entire folder containing the PMX on every load
    DESC: _texture_files does model_path.parent.rglob("*") (line 135), enumerating every file in every subdirectory under the model's parent folder, plus a second redundant rglob of the textures/ subfolder (already covered by the parent scan; duplicates only removed afterward via sorted(set())). It runs inside _read_materials during load_static_preview_model on the GUI thread. If the user picks a PMX sitting in a large directory (a model-collection root, D
    FIX: Scan non-recursively (iterdir) for the model dir and its textures/ subdir first, and only fall back to a bounded recursive scan (depth-limited or capped file count) when a texture reference fails to resolve; drop the duplicate textures/ rglob since the parent rglob already includes it.

[low|perf|preview] model_preview.py:486 statsChanged signal emitted from inside paintGL on every frame
    DESC: StaticModelPreviewWidget.paintGL ends with self._emit_stats() (line 486), so the statsChanged signal fires on every repaint - i.e. continuously during orbit/pan/zoom drags (each mouseMoveEvent calls update()). The GUI connects it straight to QLabel.setText (gui lines 4627, 4937), so every preview frame also re-sets (identical) label text and can trigger label relayout, and a widget-updating slot runs synchronously in the middle of this widget's p
    FIX: Emit stats from the places where the inputs actually change (load_model, clear_model, when _gl_error transitions) or compare against the last emitted string in _emit_stats and skip duplicates, instead of emitting from paintGL.

[low|bug|preview] model_preview.py:2038 Collision and bone overlays never render when no material triangles are present
    DESC: MaterialPreviewWidget.paintGL only calls _draw_mesh_gl when self._material_positions is non-empty (line 2038), and the collision/bone overlay drawing happens only at the end of _draw_mesh_gl (lines 2184-2185). The QPainter branch additionally returns early with the 'Scan a blend...' empty message whenever _material_positions is empty (lines 2047-2051). So if set_collision_overlay/set_bone_overlay are populated while the material preview has no tr
    FIX: Gate the GL section on having anything drawable (self._material_positions or self._collision_lines or self._bone_lines), move the overlay draw calls out of _draw_mesh_gl (or call them directly from paintGL after the mesh pass), and only show the empty-state message when all three sources are empty.

[low|bug|proportion-release] blender_export_proportion_trick.py:837 Nested Blender subprocess has no timeout and is orphaned on cancel
    DESC: run_fresh_session_verification calls subprocess.run (lines 837-847) with no timeout. If the inner Blender hangs (corrupt processed blend, addon deadlock), the outer Blender — and therefore the whole Step 9 worker — hangs indefinitely. Worse, the GUI cancel path (run_process_streamed in mmd_character_importer_core.py lines 1233-1239) only terminates the OUTER Blender process; on Windows terminate() does not kill children, so the inner Blender keep
    FIX: Pass a generous timeout (e.g. timeout=1800) to subprocess.run and convert TimeoutExpired into a RuntimeError pointing at the log. For the orphan case, consider launching the inner Blender via Popen and killing it in a finally block, or have the core use a Windows job object / taskkill /T when cancelling.

[low|bug|proportion-release] sort_release_description.py:530 Up to 44 serial DeepL requests with no rate-limit retry; 429s degrade whole languages to manual
    DESC: process_plan's translate loop (lines 530-559) issues up to 4 DeepL calls (description, title, quote, author) for each of 11 non-English languages back-to-back with zero delay, retry, or backoff. call_deepl (lines 423-427) converts any HTTPError — including 429 Too Many Requests and 456 quota-exceeded, which DeepL Free returns readily for bursts like this — straight into a RuntimeError, so the language is marked translation_failed and the user mus
    FIX: In call_deepl, retry on HTTP 429/5xx with exponential backoff (DeepL docs recommend this), and/or batch the 4 texts per language into a single request (DeepL v2 accepts repeated text parameters). In process_plan, skip languages whose row already has status 'translated_deepl' so re-runs only redo failures.

[low|bug|proportion-release] sort_release_description.py:460 Operator precedence in section_for_language can render literal 'None' into the Steam description
    DESC: Lines 460-463 use the pattern `str(plan.get("description_en") if code == "en" else row.get("description") or "")`. The `or ""` binds only to the else-branch, so for the English section a missing/None key produces str(None) == 'None', which passes the truthiness check and is emitted verbatim into the published template (same for quote_text and quote_author). default_plan populates these keys, but generate() re-reads the plan JSON from disk (line 6
    FIX: Parenthesize so the fallback covers both branches: `str((plan.get("description_en") if code == "en" else row.get("description")) or "")`, applied to all three lookups (description, quote_text, quote_author).

[low|bug|proportion-release] sort_release_description.py:376 DEEPL_API_URL override with trailing slash produces a doubled /v2/translate path
    DESC: deepl_endpoint (lines 373-378): for an override like 'https://api.deepl.com/v2/translate/' (trailing slash), endswith('/v2/translate') is False, so the code rstrips the slash and appends the suffix again, yielding '.../v2/translate/v2/translate', and every translation call 404s with a confusing endpoint in the error message.
    FIX: Normalize first: `override = override.rstrip('/')` then `return override if override.endswith('/v2/translate') else override + '/v2/translate'`.

[low|bug|proportion-release] sort_release_description.py:305 max_output_tokens 650 with a reasoning-class default model risks empty Responses output
    DESC: The Responses API payload (lines 302-306) caps max_output_tokens at 650 with no reasoning configuration, while the default model is the gpt-5 family ('gpt-5.5', line 21). For reasoning models, reasoning tokens count against max_output_tokens; a 650-token budget can be consumed entirely by reasoning, returning status 'incomplete' with no output_text/message content. The code then raises 'OpenAI response did not contain output text' (line 333) even
    FIX: Add "reasoning": {"effort": "low"} (or "minimal") to the payload and raise max_output_tokens to ~2000, and detect data.get('status') == 'incomplete' to produce a precise error message.

[low|bug|qc-compile] sort_qc_compile.py:1433 QC unconditionally references anims/reference_female and anims/proportions but analyze() never validates they exist
    DESC: base_qc_lines() always emits `$sequence reference "anims/reference_female"`, `$animation a_proportions "anims/proportions"` and the ragdoll sequence (lines 1433-1443). discover_inputs() explicitly models the anims dir as optional (line 1116 sets anims_dir to "" when missing) and prepare_qc_source()'s copytree_clean silently no-ops when step9/anims is absent (line 1320), yet analyze() (lines 1176-1190) raises validation errors for missing SMDs/Phy
    FIX: In analyze(), append a validation error (or at minimum a warning) when step9_dir/anims/reference_female.smd or anims/proportions.smd is missing, since the generated QC cannot compile without them.

[low|bug|qc-compile] sort_qc_compile.py:49 Duplicate $includemodel entries in PLAYER_ANIMATION_INCLUDES and NPC_ANIMATION_INCLUDES
    DESC: PLAYER_ANIMATION_INCLUDES lists 'f_anm.mdl' twice consecutively (lines 51-52) and 'humans/female_shared.mdl' + 'humans/female_ss.mdl' twice each (lines 56-57 and 62-63); NPC_ANIMATION_INCLUDES repeats the same two (lines 66-67 and 72-73). Every entry is emitted as a separate $includemodel line (line 1444-1445), so each compiled model carries 4 redundant include-model slots out of Source's limited animgroup budget (MAXSTUDIOANIMGROUPS). The back-t
    FIX: De-duplicate the lists (or dedupe at emission time with an ordered set) so each animation .mdl is included once.

[low|bug|qc-compile] sort_qc_compile.py:1211 analyze() 'author' parameter and --author CLI flag are silently ignored
    DESC: analyze(input_path, author=...) accepts an author argument (line 1168) and parse_args defines --author (line 3608), and mmd_character_importer_core.analyze_qc forwards it (core lines 4564-4565), but analyze() hardcodes `safe_author = "sheepylord"` (line 1211) and never reads the parameter. validate_plan() additionally rejects any other value (line 2296-2297: 'author must be exactly "sheepylord"'). If the hardcoded author is intentional branding, 
    FIX: Either honor the parameter (sanitize via safe_internal_identifier) or remove the --author flag and the analyze() parameter and document that the author namespace is fixed.

[low|bug|qc-compile] sort_qc_compile.py:3466 Distribution dir created before the containment check that rejects it
    DESC: In compose(), `distribution_output_dir.mkdir(parents=True, exist_ok=True)` (line 3466) runs BEFORE the `path_is_inside(distribution_output_dir, addon_dir)` guard (line 3467) that raises 'cannot be inside the composed addon folder'. When a user selects a folder inside the addon dir, the directory tree is created inside the composed addon before the error is raised; the stray empty directories remain in addon_dir and are subsequently copied by the 
    FIX: Perform both path_is_inside checks before calling mkdir, and only create the directory after the location is validated.

[low|bug|qc-compile] sort_qc_compile.py:3253 Hard failure on unrepairable missing-parent warnings even though studiomdl compile succeeded
    DESC: compile_with_definebone_repair() runs after compile_one() returned exit code 0, then scans the output for 'tried to access parent bone ... and failed!' warnings. If repair_definebones cannot insert any bone (the parent exists in no source SMD - e.g. referenced only by included animation data), it raises RuntimeError (lines 3253-3255), failing the whole Step 14 even though studiomdl produced a complete, usable .mdl; the same applies after the retr
    FIX: When the compile exit code was 0 and the missing parents are classified unrecoverable, demote to a warning in the report (with the bone list and log path) instead of raising, keeping the hard failure only for repairable-but-failed cases.

[low|bug|vrd-carms] blender_sort_vrd.py:1083 Explicit angle 0.0 silently coerced to 90.0 by 'or 90.0' falsy-zero pattern
    DESC: float(row.get('angle', 90.0) or 90.0) appears at line 1083 (procedural rotation blending), line 1223 (cxbone.angle for the addon project) and line 1271 (the <trigger> tolerance written to the VRD file). Because 0.0 is falsy, a user who types 0 into the Angle column of the VRD table in the GUI (mmd_character_importer_gui.py:16266 stores the raw float) gets the full 90-degree behavior instead of zero. The same pattern in global_vrd_delta_matrix lin
    FIX: Use an explicit None check: value = row.get('angle'); angle = 90.0 if value is None else max(0.0, float(value)). If 0 is meant to be invalid, clamp it in the GUI/plan normalization instead of silently substituting 90.

[low|perf|vrd-carms] blender_sort_vrd.py:141 physics.smd imported but never used - deformed in all 8 evaluated preview passes
    DESC: Unlike blender_sort_carms.py, which skips physics at import (EXCLUDED_IMPORT_NAMES = {'physics.smd'}, line 23, filtered in input_smd_files lines 122-125), blender_sort_vrd.py's input_smd_files (lines 141-146) imports every top-level SMD including Physics.smd (which step 9 does produce - blender_export_proportion_trick.py keeps a 'Physics' mesh). The object is then excluded from every consumer via EXCLUDED_INFERENCE_OBJECTS={'physics'} (lines 22, 
    FIX: Mirror the carms behavior: filter physics.smd out in input_smd_files (or delete the object right after import) instead of only excluding it at use time.

[low|perf|vrd-carms] blender_sort_carms.py:560 c_arms report JSON embeds the full triangle preview pretty-printed
    DESC: run() splices the entire preview (**preview, line 560) - up to MAX_PREVIEW_TRIANGLES=500000 sampled triangles, each with 9 coordinates, 6 UV floats and repeated texture_path/object_name strings (collect_preview lines 337-404) - into report_json, which write_json pretty-prints with indent=2 (lines 31-33), putting every float on its own line. Although c_arms geometry is smaller than the full body, a detailed model still yields a tens-of-MB report t
    FIX: Write preview data with compact separators (json.dumps(..., separators=(',', ':'))) and consider splitting the heavy model_preview into its own file (like Step 11's preview_json) so the report stays small and cheap to parse.

======================================== IDEAS ========================================
[val=7|small|ui|gui-window-C] mmd_character_importer_gui.py:11006 'Complete With Warnings' branches never clear the 'working' workflow state
    DESC: When the Blender-side validation reports not-ok, the done handlers take an else branch that only updates the progress label and skips complete_step_from_result: spine_fix_done (11005-11006), sort_bones_done (11588-11589), material_merge_done (12536-12537), bodygroup_apply_done (13317-13318). Because the 'working' flag set in the corresponding start_* handler is only cleared inside write_step_compl
    FIX: In the warnings branch call set_step_failed(step, ...) or clear_step_state(step) so the panel reflects reality, and either skip the auto-advance or surface a dialog explaining the validation warnings and what to do next.

[val=7|medium|ui|gui-window-C] mmd_character_importer_gui.py:12539 Progress titles and step-completion dialogs hardcode English despite 8-language i18n
    DESC: The in-scope progress setters and dialogs bypass the translation layer used elsewhere in the same file: set_main_progress (10337-10343) and set_bodygroup_progress/set_flex_progress (12539-12545, 13320-13326) render raw titles like 'Queued', 'Fix Complete', 'Bodygroups Complete With Warnings'; the QMessageBox.information completion dialogs at 10558, 10661, 11004, 11587, 12535, 13314-13316 and the v
    FIX: Route progress titles and the completion/validation dialog strings through self._t / translate_static_text like the rest of the UI; the catalog keys for many of them already exist.

[val=7|medium|capability|gui-window-D] mmd_character_importer_gui.py:13412 Flex and collision plans are not restored from disk after restart, forcing a full Blender re-analysis
    DESC: update_flex_output_preview (lines 13412-13424) and update_collision_output_preview (lines 14094-14107) display the on-disk paths of flex_plan.json / collision plan and analysis files and even confirm they exist, but never load them into current_flex_plan / current_collision_plan. After an app restart (or after the input row is re-set, which clears the plan at lines 13394-13400 and 14071-14092), th
    FIX: Mirror the VRD restore logic: in update_flex_output_preview / update_collision_output_preview, if plan_json and analysis_json exist, json-load them (off the GUI thread for large files), populate the tables, and enable the Apply/Validate buttons.

[val=7|small|ui|qc-compile] sort_qc_compile.py:1931 run_command buffers all studiomdl output until exit - no progress during multi-minute compiles
    DESC: run_command() uses subprocess.run with stdout=PIPE (lines 1931-1940), so nothing is emitted until the child exits; the interesting lines are only re-printed afterwards (lines 1943-1945). The GUI streams this script's stdout live (core run_process_streamed), but during a studiomdl -verbose compile of a large playermodel (which can take minutes per QC, and this step compiles main + pm + c_arms plus 
    FIX: Use subprocess.Popen and stream lines as they arrive (forwarding $definebone/$hbox/ERROR/WARNING lines immediately and writing the full log at the end), so the GUI progress log stays alive during long compiles.

[val=7|small|capability|qc-compile] sort_qc_compile.py:217 Internal identifiers ban digits, mangling or rejecting common model names like '2B' or 'miku39'
    DESC: safe_internal_identifier() strips everything outside [A-Za-z_] (lines 217-220) and INTERNAL_IDENTIFIER_RE/validate_plan enforce letters+underscore only (lines 34, 2286-2289), so 'miku39' becomes 'miku_' -> 'miku' and '2B' becomes 'B'; a plan edited to keep digits fails validation outright. Digits are fully legal in Source engine model paths, material paths, and Lua list keys, so this restriction i
    FIX: Relax INTERNAL_IDENTIFIER_RE and safe_internal_identifier to [A-Za-z0-9_] (still requiring a leading letter/underscore if desired for Lua-safety), and update the GUI filter to match.

[val=7|small|ui|vrd-carms] blender_sort_vrd.py:600 No progress output during the multi-minute inference loop - GUI log appears frozen
    DESC: The core streams this script's stdout to the GUI progress log (run_process_streamed, mmd_character_importer_core.py:4063). infer_vrd_rows (lines 600-632) emits zero log lines while scanning every bone (the dominant cost of analyze mode, see the O(bones x verts) finding), and collect_vrd_frame_previews (lines 1144-1156) is also silent across its 4 full mesh extractions. Between 'Generating standard
    FIX: Emit periodic progress lines, e.g. log(f"Scanning bone weights {i}/{total}: {bone.name}") every N bones in infer_vrd_rows, and a log per frame in collect_vrd_frame_previews ('Collecting preview frame 10/30...'). The existing log() already flushes, so the GUI will display them immediately.

[val=7|medium|capability|bones-spine] blender_sort_bones.py:444 Merge planner computes per-bone weight totals but never uses them to pick merge candidates
    DESC: automatic_plan calls vertex_group_weight_totals() (line 444) but the result is only embedded in the plan JSON for display (line 526). Chain decimation (lines 468-483) and fallback_leaf_operations (lines 530-558) select sources purely by topology/depth/name, so a heavily weighted skirt bone is merged just as readily as a zero-weight IK/physics helper bone. Preferring zero- or low-weight bones first
    FIX: Sort merge candidates by ascending weight_total (merging unweighted bones first), and within chains prefer dropping the lighter neighbor of each pair before falling back to weighted bones.

[val=6|small|capability|gui-helpers-workers] mmd_character_importer_gui.py:2404 One-click Full Import hardcodes collision quality to 'fast_preview' for the final compiled addon
    DESC: FullImportWorker step 8 calls core.analyze_collision_blend(..., quality_preset="fast_preview", ...) (line 2404). blender_sort_collision.py defines three CoACD presets — fast_preview (threshold 0.10, resolution 420), balanced, and high_quality (resolution 1000) — and the step-by-step UI exposes a collision_quality_combo (GUI lines 15011/15026), but the automated end-to-end import that compiles and 
    FIX: Add a quality_preset parameter to FullImportWorker (defaulting to 'balanced' or reading the same setting the step-by-step collision tab uses) and pass it through to analyze_collision_blend, or expose a 'collision quality' option on the main import page.

[val=6|medium|capability|gui-helpers-workers] mmd_character_importer_gui.py:2465 Full Import hardcodes author 'sheepylord' into every user's compiled addon
    DESC: FullImportWorker step 14 passes author="sheepylord" to core.analyze_qc (line 2465) and forces qc_plan["author"] = "sheepylord" (line 2474). There is no constructor parameter for author. Elsewhere the GUI also pins the field (qc_author_edit is hidden and reset to 'sheepylord' at lines 7598-7600, 8364, 8567), so every addon produced by any user of this public tool is attributed to the tool developer
    FIX: Add an author field to the main import page (defaulting to a sanitized value, e.g. derived from the Steam/Windows user or a setting), pass it through FullImportWorker into analyze_qc/qc_plan, and unhide qc_author_edit — or document clearly why the path component must stay fixed and split 'display au

[val=6|small|ui|gui-window-A] mmd_character_importer_gui.py:4252 Dependency-blocked dialog (ensure_step_can_run) is hardcoded English despite full i18n catalogs containing the strings
    DESC: ensure_step_can_run (4247-4263) builds a QMessageBox with raw English f-strings: 'Upstream step incomplete', 'Step {step} cannot run until Step {missing} is complete.', 'Jump to Step {missing}', 'Cancel'. None go through _t/translate_static_text, and apply_global_i18n cannot reach a transient dialog. The i18n catalogs already contain matching entries (en_us.json: 'upstream_step_incomplete_d75ad554
    FIX: Route the dialog strings through self._t with proper keys/placeholders (e.g. _t('workflow.upstream_incomplete_title', ...), _t('workflow.cannot_run_until', 'Step {step} cannot run until Step {missing} is complete.', step=..., missing=...)) and add the keys to the catalogs.

[val=6|small|ui|gui-window-A] mmd_character_importer_gui.py:3014 Window size/position is never persisted; app recenters at default size every launch
    DESC: apply_startup_geometry (3014-3029) always computes a fresh centered geometry and the file contains no saveGeometry/restoreGeometry or QSettings geometry keys (verified by grep). For a tool with 17 dense tabs, tables and 3D previews where users typically maximize or carefully size the window, losing the layout on every launch is a recurring annoyance, and QSettings infrastructure already exists (se
    FIX: In closeEvent (or aboutToQuit) store self.saveGeometry()/saveState() in settings_store; in __init__ call restoreGeometry if a stored value exists, falling back to apply_startup_geometry otherwise (with an off-screen sanity check).

[val=6|small|ui|gui-window-B] mmd_character_importer_gui.py:9491 All step tabs share self.worker; busy-guard silently no-ops button clicks with zero feedback
    DESC: start_full_import (9491-9492) begins with `if self.worker and self.worker.isRunning(): return`. Every step tab assigns its QThread to the same self.worker attribute (FullImportWorker 9523, ImportWorker 10525, FixWorker 10628, ... ReleaseGenerateWorker 19203), so while ANY step is running on any tab, clicking 'Import to GMod' (or any other step's Run/Analyze button, which use the same guard pattern
    FIX: When the guard trips, surface feedback (statusBar().showMessage('Another step is still running: <name>') or a message box), and/or disable the run buttons on all tabs while self.worker.isRunning(), re-enabling from a shared finished handler.

[val=6|small|ui|gui-window-B] mmd_character_importer_gui.py:7589 File-mode PathRows document folder input but Browse cannot select folders (and no multi-select for face VMDs)
    DESC: Several PathRows are created with mode 'file' although their hints explicitly invite directory input, so the Browse button (QFileDialog.getOpenFileName only, PathRow.browse lines 620-629) cannot actually pick what the hint describes: qc_gmod_row 'GMod studiomdl.exe or install folder' (7589-7595), texture_input_row 'materials.npy/materials.json or the workspace folder containing 5_sort_materials' (
    FIX: Add a 'file_or_dir' PathRow mode (two buttons or a QFileDialog with FileMode.AnyFile fallback to getExistingDirectory), and use getOpenFileNames + ';'.join for the face-VMD row so Browse appends multiple selections.

[val=6|small|ui|gui-window-C] mmd_character_importer_gui.py:10610 Early-return validation errors leave the workflow step permanently in 'Working' state
    DESC: Eight start_* handlers call set_step_working(step) BEFORE validating inputs and then return via show_error without resetting the state: start_fix (set at 10610, returns 10613/10617), start_spine_analyze (10725; 10728/10732), start_spine_fix (10955; 10958-10966), start_sort_analyze (11074; 11077/11081), start_sort_bones (11533; 11536-11543), start_material_scan (11659; 11662/11666), start_material_
    FIX: Move set_step_working after all validation checks (as start_bodygroup_analyze does), or call clear_step_state(step) on each early-return path as start_import does.

[val=6|small|ui|gui-window-C] mmd_character_importer_gui.py:10806 Spine table combo silently displays a mapping the plan does not contain for unmapped targets
    DESC: In populate_spine_table (10797-10815), when a plan entry has no source and its action is not 'add' (an unresolved target), `selected` is '' so findData returns -1, the insertItem branch is skipped (`if index < 0 and selected:`), and setCurrentIndex is never called. The QComboBox then defaults to index 0, which displays 'Add new' (for addable targets) or the first bone name alphabetically — while t
    FIX: When selected is empty, insert an explicit placeholder item such as '(not mapped)' at index 0 and select it, so the table reflects the actual plan state.

[val=6|small|ui|gui-window-E] mmd_character_importer_gui.py:16858 All step actions silently no-op while any other tab's worker is running
    DESC: Every start_* method in this region begins with 'if self.worker and self.worker.isRunning(): return' (start_texture_analyze 16858, start_texture_process 16920, regenerate_vrd_preview 16665, start_icon_generate 17572, start_qc_analyze 18343, start_qc_compile 18738, start_release_analyze 19002, start_release_generate 19177). self.worker is one shared attribute for all ~15 step tabs, and the per-tab 
    FIX: On the early-return path, show a statusBar/messagebox notice like 'Another step is still running (Step N); wait or cancel it first', or disable all step action buttons globally while self.worker is running.

[val=6|small|ui|gui-window-E] mmd_character_importer_gui.py:16913 Step 11-15 summary/progress strings are hardcoded English, bypassing the i18n system
    DESC: The app ships 8 languages (tools/i18n/*.json) and this same region uses self._t for the error dialog (19470-19479), but the user-facing summary and progress strings in Steps 11-15 are raw English f-strings set directly: texture_summary_label (16913, 16962, 17279), vrd_summary_label (16729), icon_summary_label (17650, 17654), qc_summary_label (18604, 18617), filter status texts (17942-17946, 17992,
    FIX: Route these through self._t with parameterized keys (e.g. _t('texture.summary', ...).format(...)) consistent with the existing i18n key scheme, or at minimum pass them through translate_runtime_text like the log lines.

[val=6|medium|ui|core] mmd_character_importer_core.py:1640 First-run Blender download and extraction cannot be cancelled
    DESC: ensure_portable_blender (line 1640) takes only a progress callback - no cancel_check - and neither does download_file (lines 1098-1123, ~350 MB download) nor extract_blender (lines 1161-1177, extractall of a multi-GB tree). Every step function threads cancel_check into run_process_streamed but calls setup = ensure_portable_blender(progress) without it (e.g. lines 2648, 2699, 2754), so a user who h
    FIX: Plumb cancel_check through ensure_portable_blender, check it per downloaded chunk in download_file and per extracted member (use ZipFile.infolist() loop, which also enables percentage progress) in extract_blender.

[val=6|small|ui|bodygroups] blender_sort_bodygroups.py:3139 No progress output during multi-minute analyze/apply phases, so the GUI log and progress stall
    DESC: main() prints a single 'Analyzing bodygroups' (line 3139) / 'Applying bodygroup plan' (line 3166) before the heavy work, and the next print happens only after everything finishes. The core streams stdout lines to the GUI progress callback (run_process_streamed), but during scaling, double shapekey pruning, material separation, component analysis, clustering, and preview sampling - easily minutes o
    FIX: Print short phase markers from analyze_scene/apply_plan (e.g. 'Pruning shapekeys (pre-separation)', 'Separating by material (n meshes)', 'Clustering split candidates', 'Sampling preview triangles') so the streamed log advances and the GUI can map phases to progress percentages.

[val=6|small|capability|preview] model_preview.py:543 Mesh preview is unlit: normals are parsed and uploaded but lighting is never enabled
    DESC: StaticModelPreviewWidget parses per-vertex normals from the PMX, interleaves them into _vertex_data (line 441), and enables GL_NORMAL_ARRAY with a normal pointer (lines 543-544), but GL_LIGHTING/GL_LIGHT0 are never enabled and no shader exists, so the normals have zero effect. The model renders as flat diffuse-color/texture with no shading, which makes untextured or single-color areas (and the wir
    FIX: Enable simple fixed-function lighting (glEnable(GL_LIGHTING), glEnable(GL_LIGHT0), GL_COLOR_MATERIAL, and a headlight direction in view space) in _draw_mesh_gl - the data is already in place - or drop the normal parsing/upload entirely if unlit rendering is intentional.

[val=6|small|capability|materials-textures] sort_param_textures.py:26 DDS textures excluded from texture recovery and normal-map discovery
    DESC: IMAGE_EXTENSIONS (line 26) omits .dds, which is very common in MMD models converted from games (and Pillow can decode DXT1/3/5 DDS natively). Consequences: the fuzzy missing-texture recovery in resolve_base_texture_path skips DDS candidates (suffix filter at line 355), so a model whose recorded path is stale but whose texture exists as .dds fails with 'Base texture not found'; and find_normal_cand
    FIX: Add '.dds' to IMAGE_EXTENSIONS and IMAGE_EXTENSION_PRIORITY (lowest priority), guarding conversion with a try/except for exotic DDS sub-formats Pillow cannot read.

[val=6|small|ui|bones-spine] blender_fix_spine_bones.py:1383 Validation failure raises before the report JSON is written, so structured errors never reach the GUI
    DESC: In apply mode, when validation fails the script prints the errors and raises RuntimeError (lines 1383-1387) BEFORE writing --report-json (line 1402). The core wrapper then only sees "Blender completed but did not write <report path>" (mmd_character_importer_core.py lines 2993-2996 for the equivalent sort-bones step, which also lacks the log-tail suffix that the flex step has), so the user gets a g
    FIX: Write the report JSON (including the validation block and applied operations) before raising on validation failure in both scripts, so the GUI can load and display the structured error list; optionally still skip saving the output blend.

[val=6|small|ui|proportion-release] sort_release_description.py:517 OpenAI HTTP error bodies are discarded, leaving users with unactionable warnings
    DESC: call_openai_description lets urllib.error.HTTPError propagate raw (line 313); process_plan catches it (lines 517-518) and surfaces only str(exc), i.e. 'HTTP Error 404: Not Found' or 'HTTP Error 400: Bad Request', with no response body. OpenAI's error JSON contains the actual cause (invalid model name — relevant since the default is the hardcoded 'gpt-5.5' at line 21 — bad key, quota exhausted), so
    FIX: Mirror the DeepL handling: wrap the urlopen in try/except, and for HTTPError include compact_error_body(exc.read()) plus the model name in the raised RuntimeError so the GUI warning is actionable.

[val=6|small|capability|proportion-release] sort_release_description.py:489 Feature list ignores detected pipeline metadata and can overstate addon contents
    DESC: detect_step14_metadata (lines 203-228) computes has_carms, has_vrd, has_icons and jiggle_count, and default_plan only converts their absence into warnings (lines 244-249). section_for_language then unconditionally emits all 10 hardcoded features (lines 488-490 over FEATURES, lines 38-99) — including 'First person view model (c_arms)', 'Jigglebones', and 'Ragdoll Physics for hair, clothes and skirt
    FIX: Gate features on plan['detected']: drop the c_arms line when has_carms is False and the jigglebone/ragdoll-physics lines when jiggle_count is 0, for every language list (the FEATURES rows are positionally parallel, so filtering by index works).

[val=6|small|capability|build-infra] build_mmd_character_importer_exe.ps1:209 Blender zip is bundled into the release exe without verifying the manifest's SHA-256
    DESC: Add-DataIfExists "blender-4.5.10-windows-x64.zip" "." embeds whatever file happens to sit at the repo root into the shipped executable based purely on existence — no size or hash check — even though build_assets_manifest.json carries the expected sha256/size and scripts/download_build_assets.ps1 -VerifyOnly already implements the validation. A truncated or corrupted local zip (e.g. an interrupted 
    FIX: Before bundling, invoke scripts/download_build_assets.ps1 -VerifyOnly (or inline a Get-FileHash check against build_assets_manifest.json) and fail the build with a clear message when the zip does not match.

[val=5|small|ui|gui-window-D] mmd_character_importer_gui.py:15064 Step 9 progress jumps to 24% 'Checking Bone Weights' during Blender setup, then drops back to 3%
    DESC: append_proportion_log uses the needle ('Checking ', 24, 'Checking Bone Weights') at line 15064. core.ensure_portable_blender emits 'Checking Blender 4.5 LTS downloads: <url>' (mmd_character_importer_core.py line 1077) before 'Starting Blender proportion export' is emitted (core line 3928, mapped to 3%). So at the very start of every Step 9 run the bar jumps to 24% with the misleading label 'Checki
    FIX: Make the needle specific to the bone-weight message (the script logs 'Checking {n} bone(s) for zero-weight...', so use 'bone(s) for zero-weight' or 'Checking ' + 'bone'), and/or apply the same max(self.proportion_progress_bar.value(), value) clamping used by the carms/VRD progress handlers for same-

[val=4|small|ui|gui-window-D] mmd_character_importer_gui.py:15242 Step 9 completion auto-fills the Step 10 input but not the Step 11 (VRD) input
    DESC: proportion_done propagates the final export dir to the c_arms step (`self.carms_input_row.set_value(...)` via lines 15242-15243) but never sets vrd_input_row, even though Step 11 consumes exactly the same 2_proportion_export folder (detect_step9_output handles both targets, lines 15642-15698, and collision_apply_done shows the same auto-fill pattern for Step 9 at lines 15030-15031). Users finishin
    FIX: In proportion_done, also set self.vrd_input_row.set_value(str(result.get('final_dir',''))) when hasattr(self, 'vrd_input_row'), matching the carms auto-fill.

[val=4|small|ui|gui-window-E] mmd_character_importer_gui.py:19186 Step 15 generate buttons silently run Analyze instead of the requested operation
    DESC: start_release_generate (19176-19207): when no release plan exists yet, the handler calls self.start_release_analyze() and returns (lines 19185-19187). The user clicked e.g. 'Generate English (ChatGPT)' or 'Write Template', but the app quietly runs a different operation (analyze) and drops the requested one; after analysis finishes the user must notice nothing else happened and click the button aga
    FIX: Either queue the requested operation to run automatically after analyze completes (store pending_operation and trigger it in release_analyze_done), or show a status message 'Analyzing first — click again when analysis completes.'
